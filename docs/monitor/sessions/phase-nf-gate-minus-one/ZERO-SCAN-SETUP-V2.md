# NF-RECOVERY-SETUP-v2

Status: PM PASS; desk preparation complete. Ready to request the one explicit
installation permission below. No installation or scan is authorized yet.
This replaces the consumed v1 setup, not its recorded outcome.

## One preparation target

Verify that Codex can operate the installed diagnostic's actual You page and
reload it through Inspector while automatic console capture stays attached.
Simulator proved the corrected target selection and actual component's idle
reload; it did not prove the authenticated phone route. No NFC reader starts.

The read-only phone listing at 2026-09-04T23:54:46Z again reports 0.37.0/848
and a different installation URL from the authorized recovery install. Its
device identifier matches the installation receipt; cause is unknown.
Consequently this setup requires one new explicit installation authorization.
The signed recovery app's 231 files and strict/deep signature were reverified
at 23:54:29Z against `recovery-artifact-identity.json`. No build is needed.

## Exact operator block

Send only after PM PASS and complete host-tool preparation:

> Reply **install**, then unlock your iPhone and leave it unlocked for this
> five-minute setup check. You do not need to be at the erg. No scan or further
> reply is needed.

One permission reply and one ordinary unlock. Zero rowing, heart rate, PM5
actions, scans, app taps, pasted commands, photos or receipt copying. The
five-minute cap starts when that permission arrives and includes unlocking,
installing, waiting, inspection, evidence collection and verified cleanup.
No timer spans a required human reply. All prior desk preparation is additional
cost; this is not a five-minute claim for the whole NFC effort.

## Prepared execution

1. Create a fresh private run directory and record the original start/deadline.
   Verify the manifest/signature, install the pinned recovery path once, and
   save independent app listing. Require bundle/version/build and install URL
   agreement. Do not use the controller's `prepare` mode: it pins the old app.
2. Start the unchanged controller in `capture` mode with that deadline; it
   rechecks current installation. Require a live host and WebView-loaded line.
   Save a fresh process listing and bind the exact installed executable to one
   PID. No prior run's process listing, deadline, directory or PID is reusable.
3. Invoke the prepared idle Inspector command. Select the verified process,
   exact origin and unique unoccupied WebView; navigate via the actual You link
   if needed. Require the actual enabled normal and four recovery controls.
   Emit a harmless, uniquely marked multi-line `console.info` capture check
   through that Inspector and require its complete exact output on the Mac.
   Its marker is distinct from all receipt/native-diagnostic markers; this
   is neither a fabricated NFC receipt nor a native operation. Missing or
   truncated capture stops setup before any reader can start.
   Reload once, explicitly detach/reattach, and require a new document in the
   same process with all controls ready. Never press a sample button.
4. Signal the retained live capture PID and require terminal cleanup proof.
   Inspect only allowlisted logs and retain zero-start observations. The
   normal-only classifier's missing receipt is expected here, not an idle PASS.

Reserve the final 45 seconds for controller cleanup. Locked or failed launch,
identity mismatch, occupied/missing target, missing controls, unexpected NFC
start, broken capture or expired deadline ends this setup. No retry, another
install, live repair or surprise phone instruction. PASS requires exact current
installation, actual controls/new document, zero reader starts and verified
   cleanup. Missing evidence is inconclusive; an observed wrong action is failure.

A PASS clears idle phone preparation only. The separate recovery runsheet
still needs PM PASS and explicit scan consent. Every later installation needs
fresh permission; neither this setup nor “at the erg” authorizes scanning.

## PM and command pins

The concrete [command card](RECOVERY-COMMAND-CARD.md) and
[host rehearsal](RECOVERY-HOST-PREPARATION.md) are complete, including final
hashes and code-review PASS. The canary uses four lines, maximum 3,127
characters, comparable to the retained real receipt's 3,071-character frames.
It and the idle reload passed through actual Simulator Inspector/PTY capture;
the phone transport/route are the declared zero-scan feasibility questions.
`/root/walk_pm`: **PASS — NF-RECOVERY-SETUP-v2**, for this exact one-install,
one-unlock, zero-scan block. Final hashes match the reviewed files; no further
desk preparation or protocol change is required before requesting permission.
One install reply, then one ordinary unlock; five minutes total from permission
through cleanup. No erg presence, scan, pasted commands or further reply.

The separate four-case recovery protocol needs no scope revision, but remains
NOT READY for an erg invitation until this actual phone setup passes current
installation, authenticated controls, complete capture, document reload, zero
reader starts and verified cleanup. Scan consent remains separate.
