# RC-37 + End-button terminate — implementation report

Spec: `docs/superpowers/specs/2026-08-27-link-authority-design.md` (rev 5, `[R5]`).
Brief: `docs/superpowers/plans/2026-08-27-rc37-brief.md`.
Worktree: `/Users/james/projects/github/jamesawesome/Ergomatic/.claude/worktrees/rest-countdown`, branch `link-authority`.

## What was built

**Part 1 — the detector** (`app/src/monitor/driver.ts`). A second, persistent
structure comparator (`armedWatch`/`armedWatchFired`) that runs on every
GENERAL_STATUS tick outside a verify phase (`pendingVerify === null`), for as
long as `armedProgram() !== null`. Gated on `state === "armed"`; compares
against `expectedArmedStructure(armedProgram())`, never a literal `8`. Uses
BOTH `STRUCTURE_MISMATCH_TICKS` and `STRUCTURE_MISMATCH_WINDOW_MS`. Emits a
new `MonitorEvent` (`{ kind: "programDropped" }`, added to
`domain/monitor/types.ts`) and logs `structure-left` once both thresholds are
met; logs `structure-mismatch-recovered` on either exit from a started streak
(self-correction or leaving "armed") that never reached both thresholds.
Reset alongside the driver's other per-run state on every successful
`program()`. Idempotent (`armedWatchFired`) against stale post-detection
ticks. ~130 lines net, not ~50 — see "What I found that contradicted the
brief" below for why.

**Part 2 — the consumer** (`app/src/monitor/useMonitorSession.ts` +, contrary
to the brief's file estimate, `app/src/workout/ConnectedInterstitial.tsx` and
`domain/monitor/types.ts`). On `programDropped`, guarded to
`phase === "programming" || phase === "ready"` (the same states `cancel()`
itself is valid from): unsubscribes, drops `driverRef`, disconnects
best-effort — **no terminate call** — and flips `phase`/`programDropped` in
one `update()`. A new `session.programDropped` boolean is exposed;
`ConnectedInterstitial.tsx` gained one `useEffect` that calls `onExit()` when
it flips true — the one step the hook itself cannot do, since `onExit` never
reaches it. No row: a pre-row session opens no record at all
(`createMonitorRun`'s single call site, gated on `phase === "ready"`).
Nudges survive because `WorkoutDetailView` never unmounts.

**Part 3** (`useMonitorSession.ts`, `endSession`): `|| linkGone` deleted from
the terminate guard, exactly as specified — one line, two tests.

## What I found that contradicted the brief — reported, not silently worked around

1. **Part 1 is ~130 lines, not ~50.** The estimate undercounted the doc
   comments this codebase's own convention requires (every constant/branch
   here carries a "why", not just a "what" — see the file's existing style)
   and the near-miss handling on BOTH exits (self-correction and
   leaving-armed), which the brief's own prose asks for but its line
   estimate didn't budget.
2. **Part 2 could not stay inside `useMonitorSession.ts`.** Reaching
   `onExit()` — the destination the brief itself names as load-bearing
   ("the destination is `WorkoutDetail`... never `onEnded`'s post-session
   flow") — is structurally impossible from inside the hook: `onExit` is a
   prop the hook never receives, and setting `phase: "ended"` to route
   through the EXISTING "ended" handling would land on `onEnded`'s post-
   session log screen instead — the wrong destination, explicitly forbidden
   ("Do not invent a new screen... that bypasses it"). The only way to reach
   the SAME `onExit()` handleCancel reaches was a new `session.programDropped`
   boolean plus one small `ConnectedInterstitial.tsx` effect that calls
   `onExit()` directly. This also required a new `MonitorEvent` member
   (`domain/monitor/types.ts`) as the driver→hook signalling seam — there is
   no other way for the driver to tell the hook anything. Neither touches a
   stored shape, a pgEnum, or a migration — the "no stored shape" constraint
   is intact; only the *file* constraint was too narrow.
3. **"Carry EXPECTED and OBSERVED (all four fields)"** — `ArmedStructure`
   has exactly three fields (`workoutType`, `workoutDurationRaw`,
   `workoutDurationType`); there is no fourth. I reused the EXISTING
   `describeStructureMismatch` helper, whose own doc comment says "all
   three fields explicitly" — the same idiom `verifyArmed`'s own
   `structure-mismatch` entry already uses, so a `structure-left` entry
   reads identically to its sibling. Read as an imprecise brief, not a real
   fourth field to invent.
4. **`endSession()`'s own guard (`cancel()`'s twin) needed no change** —
   confirmed by reading, not assumed: `cancel()`'s `armed` check already
   depends on `driver !== null`, and the `programDropped` handler nulls
   `driverRef` itself, so no interaction between the two paths was possible
   to introduce.

Everything else in the brief was followed verbatim: no stored shape, no
pgEnum, no migration, no new copy, the `armed` gate kept, both constants
used, compare against what we sent.

## Gates run, from `app/`

| Gate | Result |
| --- | --- |
| `pnpm lint` | clean |
| `pnpm typecheck` | clean |
| `pnpm format:check` | clean (ran `pnpm format` once first; only my own new/touched files reformatted) |
| `pnpm test --project unit --project client` | 194 files, **5441 passed**, 1 skipped (baseline was 5420; net +21 new tests: 7 driver.test.ts synthetic + 1 added mid-mutation, 6 structureWatchReplay.test.ts, 1 structureWatchSessionReplay.test.ts, 3 useMonitorSession.test.ts, 2 ConnectedInterstitial.test.tsx, 1 WorkoutDetail.test.tsx) |
| `pnpm e2e` | **417 passed**, 0 failed (mandatory — this diff touches `app/src/`) |
| `pnpm screenshots` | **not run**, per the brief — no copy or layout change; confirmed by inspection (no CSS/JSX render-shape edits, only a `useEffect` with no visual output) |

Full unit+client and driver-suite runs were re-confirmed green after every
mutation was restored (see below) — the numbers above are the final,
clean-tree state.

## Per-file coverage (from `pnpm test:coverage`, HTML report at `app/coverage/`, per the briefing's own "text reporter omits some directories, HTML is authoritative")

| File | Stmts | Branch | Funcs | Lines | Uncovered |
| --- | --- | --- | --- | --- | --- |
| `app/src/monitor/driver.ts` | 99.45% (732/736) | 97.62% (493/505) | **100%** (92/92) | 99.71% (694/696) | lines 4049-4050 — **pre-existing**, an already-documented "UNREACHABLE today and uncovered on purpose" branch in `flushTerminateObservations` (nothing to do with RC-37; confirmed by reading the comment at that line before and after my change — line numbers shifted but the branch is unchanged) |
| `app/src/monitor/useMonitorSession.ts` | 99.03% (511/516) | 95.93% (283/295) | 98.57% (69/70) | **100%** (477/477) | one pre-existing uncovered function (`fstat-no` at the HTML report's line 9408 — a `clearTimeout` cleanup closure, unrelated to `programDropped`) |
| `app/src/workout/ConnectedInterstitial.tsx` | **100%** (73/73) | **100%** (70/70) | **100%** (21/21) | **100%** (66/66) | none |
| `domain/monitor/types.ts` | 0/0 | 0/0 | 0/0 | 0/0 | type-only file, nothing executable — normal for a pure `.d.ts`-shaped module |

My new `programDropped`/RC-37 code inside `driver.ts` and `useMonitorSession.ts`
is **100% function-covered** and every branch I introduced is exercised (the
one useMonitorSession.ts branch found uncovered by an early coverage run —
the `phase !== "programming" && phase !== "ready"` early-return — was closed
by adding a dedicated stub-transport test; see mutation list below).
`WorkoutDetail.tsx` itself was not modified (only its test file), so its
own pre-existing coverage gap is outside this task's scope.

## Self-mutation transcripts

Every mutation below was applied, run against its covering test(s), watched
fail for the stated reason, then reverted and re-confirmed green.

### driver.ts — Part 1

1. **Armed gate** (`const armed = toMonitorFrame(...).state === "armed"` →
   `const armed = true`). Red: the dedicated armed-gate test AND
   `walk-2026-08-25/rests-finished`'s own negative-corpus test — a REAL rest
   boundary now falsely fired `structure-left` (`5 consecutive armed
   tick(s) over 2070.9ms... observed durationRaw=500 durationType=128;
   expected durationRaw=6000 durationType=0`). Re-ran with the same
   mutation against the **pyramid** capture too (added mid-task, see
   below): also caught it, on interval 2's own boundary (`durationRaw=700`
   vs expected `300`) — two independent real captures both convict the same
   mutant. Restored; both green.
2. **Window half removed** (`mismatchStreak >= TICKS && heldMs >= WINDOW`
   → `mismatchStreak >= TICKS`). Red: "pins BOTH constants (1 of 2)" (a
   600ms-held 3-tick streak wrongly fired) and "the near-miss (1 of 2)"
   (self-correction test — the streak fired before the correcting tick
   could ever arrive). Restored; green.
3. **Tick-count half removed** (`... && heldMs >= WINDOW` → `heldMs >=
   WINDOW` alone). **First attempt found a real gap**: the existing "pins
   BOTH constants (2 of 2)" test (alternating payloads) did NOT catch this
   — an alternating payload resets `mismatchSince` on every tick regardless
   of whether the tick-count half is even checked, so `heldMs` never clears
   the window either way. Added a NEW test ("pins BOTH constants (3 of 3)":
   the SAME wrong structure, 2 ticks, 2500ms apart) that isolates the other
   shape. Re-ran the mutation: the new test correctly went red (`2
   consecutive armed tick(s) over 2500ms`), the old one stayed green
   (proving it tests something real, just not this). Restored; both green.
   **This is documented per the mutation-probe convention — the gap the
   probe found is now closed, not just noted.**
4. **`armedWatchFired` guard removed** (`else if (!armedWatchFired)` →
   `else if (true)`). Red: the driver-level "fires... exactly once" test
   (2 entries instead of 1) AND the real-capture positive replay (20
   `structure-left` entries instead of 1, across the 112-frame mismatch —
   the idempotence guard matters most exactly on a real long-held
   mismatch). Restored; both green.
5. **Near-miss log on the leaving-armed exit disabled**
   (`if (armedWatch.mismatchStreak > 0)` → `if (false)` on the `!armed`
   branch). Red: "the near-miss (2 of 2)" (0 entries instead of 1).
   Restored; green.
6. **Near-miss log on the self-correction exit disabled** (same guard on
   the matching-structure branch → `if (false)`). Red: "the near-miss (1 of
   2)" (0 entries instead of 1). Restored; green.
7. **Per-run reset removed** (`armedWatch = {...}; armedWatchFired = false;`
   deleted from `program()`'s success block). Red: the dedicated reset
   test (a leftover 2-tick streak from an OUTGOING program, held long
   enough that its own window was already spent, combined with the
   INCOMING program's first mismatched tick to fire at streak=3 on stale
   history — `2 consecutive... over 2600ms` in the mutant's own error).
   Restored; green.

### useMonitorSession.ts — Parts 2 & 3

8. **Part 3 reverted** (`if (driver === null) return;` →
   `if (driver === null || linkGone) return;`). Red: "End terminates the
   machine even with frameSilence latched" (`expected 5 to be greater than
   5` — no new wire write). **Green, unaffected**: "the existing link-up
   behaviour is unchanged" and all 196 other tests in the file — confirms
   the fix's blast radius is exactly the `frameSilence`-while-`phase-live`
   case, nothing else. Restored; 198/198 green.
9. **Part 2 consumer guard removed** (`if (phase !== "programming" && phase
   !== "ready") return;` → `if (false) return;`). Red: the dedicated
   stub-transport guard test (`phase` came back `"idle"` instead of staying
   `"ended"`) — and ONLY that test; the real end-to-end replay test stayed
   green (it exercises the guard's TRUE branch, not this one). Restored;
   green.
10. **Spurious terminate injected** into the `programDropped` handler
    (`bestEffort(driver.disconnect())` →
    `bestEffort(driver.terminate().finally(() => driver.disconnect()))`).
    **First attempt found a real test-design flaw**: my original
    `structureWatchSessionReplay.test.ts` assertion (`writesAfterDetection`,
    a before/after split around `program()`'s own resolve) did NOT catch
    this — `replay.run()` delivers every remaining event, mismatch
    included, before `program()`'s promise is ever awaited, so both
    snapshots landed on the far side of the spurious write and the delta
    was always 0. Rewrote the assertion to compare TOTAL wire writes
    against the capture's own recorded tx count on
    `RECEIVE_CHARACTERISTIC_UUID` (5, exact) instead. Re-ran the mutation:
    correctly red (`expected 6 to be 5`). Restored; green. **Same
    self-mutation discipline as #3 — the probe that didn't bite got fixed,
    not shipped.**

### ConnectedInterstitial.tsx

11. **RC-37 exit effect disabled** (`if (session.programDropped) onExit();`
    → `if (false && session.programDropped) onExit();`). Red: the
    dedicated ConnectedInterstitial-level test (`onExit` never called) AND
    the higher-level `WorkoutDetail.test.tsx` nudges test (the screen never
    left "Ready when you pull", so the later `getByRole("button", {name:
    "Connect"})` found nothing at all). Restored; both green (151/151
    across the two files).

## The negative corpus — widened to five captures, per James's reinforcement

The brief named four captures; the coordinator's mid-task message named a
fifth (`walk-2026-08-18-metrics/pyramid-pm5-recording-1787090555458.jsonl.gz`,
Phase CM's own exit walk) and asked for per-capture numbers, not just a pass.
Both are now in `app/src/monitor/structureWatchReplay.test.ts`.

**Every program transcription is BYTE-VERIFIED**, not guessed: decoded this
session with a throwaway script (`/private/tmp/.../scratchpad/decode-program.mts`,
never committed) built on this repo's own `domain/monitor/csafe.ts#parseFrame`
+ `domain/monitor/pm5/framer.ts#reassemble` — the exact inverse of
`pm5/commands.ts#buildProgrammingSequence`. Every capture's `divergences`
assertion passed on the FIRST run for all six (five negative + the one
positive) — proof the transcriptions are byte-exact, not just plausible.

| Capture | Program (byte-decoded) | Armed frames | `structure-left` | `structure-mismatch-recovered` |
| --- | --- | --- | --- | --- |
| `walk-2026-08-25/rests-finished` | w 1' r1 / w 500m r1 / w 1' | 68 | 0 | 0 |
| `walk-2026-08-25/smoke-terminated` | w 1' (Menu-terminated ~31s) | 31 | 0 | 0 |
| `walk-2026-08-27/boundaries-terminated` | 3×250m, r1 after interval 1 only (Menu-terminated in interval 3) | 181 | 0 | 0 |
| `walk-2026-08-23/keystone` | 2×250m, no rest | 27 | 0 | 0 |
| `walk-2026-08-18-metrics/pyramid` | 300m r1 / 700m r1 / 300m (distinct targets) | 167 | 0 | 0 |
| **Negative corpus total** | | **474** | **0** | **0** |
| `walk-2026-08-27/menu-at-ready` (POSITIVE) | 240s r1 / 240s r1 | 156 | **1** | 0 |

`armedFrameCount` is an INDEPENDENT oracle — computed directly off each
capture's own raw 0x0031 bytes via `parseGeneralStatus`/`toMonitorState`,
never off anything the driver-under-test decided, mirroring the "300 armed
frames" methodology the design spec's own §1 cites (applied per-capture here
rather than only as a corpus-wide total). 474 armed frames across five
genuinely healthy sessions, **zero** false detections and **zero** near-miss
recoveries — the margin is clean, not merely lucky: no capture ever produced
even a single armed-tick mismatch to recover FROM. **There is nothing here
for the hardware walk's near-miss reading to report** (the design spec's own
instruction, §1b: "if a rest-bearing piece produces
`structure-mismatch-recovered` entries at all, say so... it is the
measurement that decides whether the thresholds are comfortable or lucky")
— on this evidence they read comfortable, not lucky, though this is REPLAY
evidence only; the design spec's own exit criterion 5 (a rowed hardware leg)
is what actually settles it, and that walk has not happened.

The positive capture (`menu-at-ready`) fires exactly once, on
`3 consecutive armed tick(s) over 2000ms` reaching the same threshold
combination the README's own decode recorded holding for 112 frames /
56.4s in the real walk — this replay only needs the first 3 ticks past both
thresholds to prove the detector, not the full 112.

## What was NOT done (correctly, per the brief)

- No PR opened, no merge, no worktree removal.
- No hardware walk (design spec exit criterion 5 — explicitly gates the
  merge, not this task).
- `pnpm screenshots` not run (no copy or layout change).
- No `.claude/agent-briefing.md`-style ledger update proposed — this is not
  a phase-boundary or triad-weight change per the standing rules (no stored
  shape, no number-meaning change, no auth); it is inline task work under an
  existing brief, and the phase-close antagonist/PM gates apply at the
  design spec's own next milestone, not here.

## Files touched

- `app/domain/monitor/types.ts` — new `MonitorEvent` member (`programDropped`)
- `app/src/monitor/driver.ts` — the detector (Part 1)
- `app/src/monitor/driver.test.ts` — 8 new tests (7 synthetic + the gap-closing 8th)
- `app/src/monitor/structureWatchReplay.test.ts` — new file, 6 tests (5 negative-corpus + 1 positive, driver-level)
- `app/src/monitor/structureWatchSessionReplay.test.ts` — new file, 1 test (hook-level, real-capture end-to-end)
- `app/src/monitor/useMonitorSession.ts` — the consumer (Part 2) + the one-line End fix (Part 3)
- `app/src/monitor/useMonitorSession.test.ts` — 3 new tests (End-terminates, link-up-unchanged, consumer-guard)
- `app/src/workout/ConnectedInterstitial.tsx` — the `onExit` wiring effect
- `app/src/workout/ConnectedInterstitial.test.tsx` — 2 new tests
- `app/src/workout/WorkoutDetail.test.tsx` — 1 new test (nudges survive, full component stack)
- `app/src/workout/ConnectedSurface.screens.test.tsx`, `ConnectedSurface.test.tsx`,
  `connected/ConnectionLogSheet.test.tsx`, `connected/PaneGrid.test.tsx` — one-line
  `programDropped: false` fixture additions (type-forced by the new `MonitorSession` field)

No iOS build-stamp files touched (`project.pbxproj`, `Info.plist`, `Package.swift`
untouched by this task — confirmed via `git status`).

---

## Fix round 1 (spec-compliance review: PASS, one MUST-FIX + four optionals, all five taken)

The reviewer independently decoded all 474 armed frames from the gzips
itself — the findings below are treated as verified, not re-derived from
scratch, except where noted I re-checked independently anyway.

### 1. MUST-FIX — the re-arm window was unguarded, and the comment claimed it wasn't

**Confirmed by reading, before touching anything**: `program()`'s four
phases are `sendPrepare()` → `waitForPrepareSettle()` → `sendSequence()` →
`verifyArmed()` (`driver.ts`). `pendingVerify` is non-null only in the
LAST phase; through the first three it is `null`, and `armedProgram()`
still returns the OUTGOING program — `activeRun` is replaced only on the
success path, after `verifyArmed()` resolves. My original comment claimed
the window was closed by the `pendingVerify` check; it was not, and — per
recurring failure #21 — a comment claiming a gate holds is worse than no
comment, because it retires the suspicion that would have found the bug.

**TDD, exactly as instructed**: wrote the re-arm test FIRST against the
unfixed code, ran it, confirmed RED —

```
AssertionError: expected [ { seq: 18, …(3) } ] to strictly equal []
detail: "3 consecutive armed tick(s) over 2000ms reporting a structure
  that does not match the sent program — observed workoutType=1
  durationRaw=0 durationType=128; expected workoutType=8 durationRaw=6000
  durationType=0 (the sent program's interval 0)"
```

— i.e. a re-arm mid-flight (a SECOND `program()` call, before its own
`verifyArmed`) fired `structure-left` on the OUTGOING program's now-stale
expectation, exactly as the reviewer predicted: `sendPrepare()`'s own
Terminate cycles the machine Terminate → Rearm → WaitToBegin holding its
unprogrammed default (`workoutType=1/durationRaw=0/durationType=128` — RC-
37's own positive shape) for as long as the real `sendSequence` send takes.

**Fix**: `} else if (!armedWatchFired && !programInFlight) {`
(`programInFlight` was already in scope, gating re-entrant `program()`
calls). Re-ran the test: green. Rewrote the comment to state the true
mechanism and cite the fix-round finding by name, rather than repeat the
false claim.

**Test added** (`driver.test.ts`, "MUST-FIX: a re-arm in flight..."):
re-arms with a genuinely DIFFERENT program (not the same one, per the
reviewer's own request), injects the wrong-structure ticks mid-send
(before `verifyArmed` is ever called), asserts silence during that window,
then lets the re-arm resolve and asserts the watch resumes normally
afterward (three more wrong ticks against the NEW program's own
expectation fire as usual) — so the test also proves the guard is scoped
to the in-flight window, not a permanent suppression. The red-then-green
cycle above IS this test's own mutation proof; re-verified by re-running
the full `driver.test.ts`/`structureWatchReplay.test.ts`/
`structureWatchSessionReplay.test.ts` suite (258 passed) after the fix.

### 2. Throttled the near-miss entry across streak cycles

Added `STRUCTURE_RECOVERED_LOG_CAP = 5` and a per-run counter
(`armedWatchRecoveredLogged`, reset alongside `armedWatch`/
`armedWatchFired`), routed through one shared helper
(`recordArmedWatchRecovered`) so the cap can never drift between the two
`structure-mismatch-recovered` call sites (self-correction, leaving
"armed"). The cap bounds what reaches the RING; the detector's own state
machine (`armedWatch`'s reset) is unconditional regardless of whether the
cap already fired — a capped-out run still tracks mismatches correctly, it
just stops writing near-miss entries about ones already demonstrated.

**Test** ("caps 'structure-mismatch-recovered' entries at 5 per run"): 8
mismatch/recover cycles (a bouncing wire), asserts exactly 5 entries
reached the ring, never 8, and that `structure-left`/`programDropped`
never fired (every streak is 1 tick). **Mutation**: removed the cap check
— red (`expected [...] to have a length of 5 but got 8`). Restored — green.

### 3. What the negative corpus actually proves — corrected

**The original report's negative-corpus section implied a broader sweep
than the corpus actually performs. This section corrects it explicitly,
and `structureWatchReplay.test.ts`'s own `describe` block now carries the
same correction as a permanent code comment, not just here.**

Independently verified this session (own script, not a re-statement of the
reviewer's number): every armed run in all SIX captures (five negative,
one positive) is a single contiguous block at the head of that capture's
own General Status stream — index `[0..N-1]` — and ZERO armed frames occur
at or after the first rest state in any of them:

```
rests-finished:        armed [0..67]   firstRest=185  armedAtOrAfterRest=false
smoke-terminated:      armed [0..30]   firstRest=none armedAtOrAfterRest=false
boundaries-terminated: armed [0..180]  firstRest=312  armedAtOrAfterRest=false
keystone:               armed [0..26]   firstRest=none armedAtOrAfterRest=false
pyramid:                armed [0..166]  firstRest=320  armedAtOrAfterRest=false
menu-at-ready:          armed [0..155]  firstRest=none armedAtOrAfterRest=false
countdownPauseFrames=0 in all six captures
```

**So "zero false detections at a rest boundary" is proved by the `armed`
GATE and its own dedicated mutation (`driver.test.ts`'s "pins the armed
gate" test, which fabricates a rest-boundary-shaped mismatch directly and
confirms the gate suppresses it) — NOT by the negative corpus**, which
never places an armed frame anywhere near a rest boundary to sweep. This
is exactly why the design spec's own exit criterion 5, leg 2 (a
rest-bearing piece, ROWED, not replayed) is the actual gate, and it has
not happened. Also recorded: `COUNTDOWNPAUSE` (state 2) is admitted as
`"armed"` by `WORKOUTSTATE_TO_STATE` — inherited from `verifyArmed`, not
introduced by this task — and appears in NONE of the six captures; an
unobserved armed sub-state.

Both facts are now a permanent comment on `structureWatchReplay.test.ts`'s
`HEALTHY_CAPTURES` describe block, so this correction survives past this
report.

### 4. Pinned the per-capture armed counts exactly

`HEALTHY_CAPTURES` now carries an `armedFrameCount` field per capture (68,
31, 181, 27, 167) and the assertion changed from `toBeGreaterThan(0)` to
`toBe(capture.armedFrameCount)`; the positive capture's own count (156) is
pinned the same way; a new test asserts the five-capture total is exactly
474. **Mutation**: changed `rests-finished`'s pin from 68 to 67 — both the
per-capture assertion AND the total-sum assertion went red
(`expected 68 to be 67`, `expected 473 to be 474`); restored, both green.

### 5. Pinned that `structure-left` reaches the stash

`structureWatchSessionReplay.test.ts`'s `runReplay()` now captures
`unmount` from `renderHook` and calls it after the replay (standing in for
the real `onExit()`-triggered unmount `ConnectedInterstitial` would cause),
then reads `sessionStorage["ergomatic:last-monitor-log"]`. New test: the
JSON-parsed stash contains exactly one `structure-left` entry. **Mutation**:
removed the `stash()` call from `teardown()`'s immediate path (`useMonitorSession
.ts`) — red (`expected null not to be null`) on this new test, sibling test
unaffected; restored — both green.

### Gates, fix round 1

| Gate | Result |
| --- | --- |
| `pnpm lint` | clean |
| `pnpm typecheck` | clean |
| `pnpm format:check` | clean |
| `pnpm test --project unit --project client` | 194 files, **5445 passed**, 1 skipped (net +4 over the prior round's 5441: the MUST-FIX test, the cap test, the total-sum test, the stash test) |
| `pnpm e2e` | **not run** — this fix round touched only `app/domain/monitor/types.ts` (untouched, confirmed by `git diff --stat`), `app/src/monitor/driver.ts`, `driver.test.ts`, `structureWatchReplay.test.ts`, `structureWatchSessionReplay.test.ts` — nothing under `app/src/workout/`, and the instruction scoped e2e to that condition |
| `pnpm test:coverage` (full, incl. server/integration) | one unrelated flake on first run (`server/routes/data.test.ts`'s "GET with no plan selected", 401 vs 200 — passes in isolation, 338/338, and passes on a clean re-run of the full suite too: not caused by this diff, which touches nothing under `server/`) |

### Per-file coverage, fix round 1

| File | Stmts | Branch | Funcs | Lines | Uncovered |
| --- | --- | --- | --- | --- | --- |
| `app/src/monitor/driver.ts` | 99.46% | 97.64% | **100%** | 99.71% | lines 4072-4073 — the SAME pre-existing documented gap as the first round (line numbers shifted by the new code; re-read the comment at the new location to confirm it's unchanged: "UNREACHABLE today and is uncovered on purpose") |
| `app/src/monitor/useMonitorSession.ts` | 99.03% | 95.93% | 98.57% | 100% | unchanged from round 1 — this file's own diff is net zero this round (the stash mutation was applied and reverted) |

Every branch fix round 1 added (`!programInFlight`, the recovered-log cap,
the pinned counts, the stash assertion) is covered — confirmed via the
HTML report's own `cstat-no`/`missing-if-branch` markers in `driver.ts`,
all 8 of which are pre-existing lines (`clampedKeysLogged`, `stamp`,
`activeRun`, two `lastIndex < 0` guards) unrelated to this task, none of
them touching the RC-37 code this round changed.

### What changed since round 1's summary (for anyone re-reading only the top)

- `driver.ts`: `!programInFlight` added to the armed-watch gate (the
  MUST-FIX); a shared `recordArmedWatchRecovered()` helper with a 5-per-run
  cap; the misleading gate comment rewritten.
- `driver.test.ts`: +2 tests (re-arm-in-flight, recovered-cap).
- `structureWatchReplay.test.ts`: exact per-capture/positive/total armed-
  frame counts pinned; a permanent comment stating what the corpus does
  and does not prove, and the `COUNTDOWNPAUSE` observation.
- `structureWatchSessionReplay.test.ts`: +1 test crossing the
  unmount-to-stash boundary.
- No change to `useMonitorSession.ts`, `ConnectedInterstitial.tsx`, or
  anything under `app/src/workout/` this round.
