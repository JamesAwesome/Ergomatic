# Deliberate deviations from the design handoff

The handoff in this directory is the UI/UX authority EXCEPT where listed
here. These are intentional product decisions (spec:
`docs/superpowers/specs/2026-07-28-differentiation-design.md`) — do not
"fix" screens back to the handoff's literals.

| Handoff shows | Ergomatic builds | Why |
|---|---|---|
| Pain 1–10 (`PAIN 5/10`, 10-segment bars, 1–10 pickers) | Pain **1–5**: `PAIN n/5`, 5-segment bars, five 44px picker cells | Differentiation; simpler scale |
| `PAIN ≤5` library filter chip | Five individual PAIN cells (1–5), multi-select union, inside the FILTER sheet (`FilterSheet.tsx`) — not a single `≤` threshold control at all | Task 4 (ui-fix round), DESIGN.md's own words: "the old single PAIN ≤3 chip could not express 'give me something hard'". Superseded the original `PAIN ≤3` deviation this row used to record (a single threshold's own midpoint pick no longer applies once PAIN is multi-select) |
| Flat row of up to eleven filter chips wrapping raggedly, plus an accent `ALL` clear-everything chip (`FilterChips.tsx`, deleted) | One `FILTER ⌄` chip (ink border, 44px) plus a plain count at rest; tapping it opens a sheet (`FilterSheet.tsx`) holding all five groups (TYPE/TIME/PAIN as 4/4/5-cell grids, LAST DONE+SOURCE sharing a line as 2-cell grids) with a live-counting `.button-l1` "Show N workouts"; active filters render back on the Library screen as a token row (`filterTokens.ts`: one token per group, not per band — a contiguous selection collapses to a range like `PAIN 4–5`, a non-contiguous one lists as `PAIN 1, 4`), each token a 44px pill (type tokens fill the type colour, every other kind fills ink) with its own 44×44 `✕`, plus a `CLEAR ALL` accent control on the count line, present only while something is active | Task 4 (ui-fix round), DESIGN.md's own words: the ragged eleven-chip wrap "mixed three different selection semantics... behind one visual and one ARIA vocabulary" and wrapped mid-group at 390px, so two chips of opposite behaviour (e.g. `HARD` and `≤60′`) rendered pixel-identical. The sheet groups filters by their actual selection semantics (single-select, multi-select, mutually-exclusive pair) instead of nine identical-looking flat siblings; the token row keeps what's active visible on the main screen without the sheet staying open, and the count always reads off the tokens so the row and the count can never disagree |
| Difficulty `Introductory / Moderate / Advanced` (chips `INTRO/MODERATE/ADVANCED`) | `Easy / Medium / Hard` (chips `EASY/MEDIUM/HARD`) | Differentiation |
| Sample data: 11 book-derived workouts ("Lucky Penny", …) | Original ~300-workout generated library (original names/structures), seeded globally at boot, converged by title on every boot | Content policy |
| "11 OF 375 ENTERED" library counter | Plain count of the user's library, no fixed denominator: `N WORKOUTS` at rest, `N OF M SHOWN` when filtered (`Library.tsx`) | 375 is the book's scale, not ours. Task 4 (ui-fix round) retired the literal word "ENTERED" too — DESIGN.md: it "described the data-entry backlog rather than anything the rower needs" |
| `ink-4` `#8a8478` for mono labels (e.g. "33D AGO", step sub-lines) | `ink-4` **`#6f6a5f`** | `#8a8478` measures 3.29:1 on `--page` and 3.65:1 on `--surface` — below the handoff's own WCAG AA (≥4.5:1) requirement for this text size. `#6f6a5f` measures 4.76:1 on `--page` and 5.29:1 on `--surface`, passing AA while preserving the four-tier ink hierarchy (ink → ink-2 → ink-3 → ink-4) rather than collapsing small labels into ink-3. AA is the hard requirement; the literal hex was not. The builder redesign's own step-index numeral (`StepCard.tsx`'s `.step-card-index`) uses this same substitution for the same reason — its handoff explicitly calls out `#8a8478` there too (~3.4:1) and says to move it to `#6f6a5f` if axe flags it; it does, so it's already `#6f6a5f` in `tokens.css`, and `e2e/design.spec.ts`'s accordion-states sweep pins the resolved colour structurally |
| `+ ADD ROW`'s mockup `nwRows` model has no `kind` field at all — every added row is implicitly a work row | A single `+ ADD STEP` control, work rows only — no `+ WARM-UP` any more. Warm-up leaves the workout entirely: it's read from the rower's preferences (`GET /api/prefs`'s `warmupMinutes`) and shown as a line directly above the step list — reading as an implicit step 0, since that's what actually happens at session start — rather than authored per workout or saved on it | `+ WARM-UP` existed for one phase because the domain distinguishes warm-up, work, and rest steps and every starter workout in the book opens with one — a builder that could only ever add work rows couldn't author a warm-up from scratch. A later round of device feedback found authoring one per workout was pointless friction: every session uses the same easy warm-up regardless of which workout follows it, so it moved to a one-time preference instead of a per-workout row. `addBlankStep`'s "+ ADD STEP" (`builderState.ts`) and `StepEditor.tsx`'s `wu`/`r` minutes-only branch stay in the code — bulk import and edit-mode `fromWorkout` can still produce/keep a stored `wu`/`r` step, and a pasted or previously-saved workout that already has one must stay editable — it's just not authorable from a blank builder any more. There is likewise no `+ REST` control: rest is authored via a work row's own REST stepper instead, for the same "not reachable from a create-mode button, but still editable" reason |
| The builder-redesign handoff's own `rows` state models **only work steps** — no `kind` field at all (just `dur`/`unit`/`ref`/`off`/`spm`/`rest`) | `wu`/standalone `r` steps still render, are still editable, and still round-trip through Save unchanged — `StepEditor.tsx`'s minutes-only editor branch (header, DUR, DONE — no PACE/SPM/REST/TARGET), `builderState.ts`'s `stepToRow`/`toSteps` | Same standing reason as the `+ ADD ROW` row above, restated because this redesign's own handoff repeats the omission rather than fixing it: the domain distinguishes warm-up/work/rest steps and every one of the generated 300-workout library's entries (plus anything bulk-imported) can contain a stored `wu`, so a builder that silently dropped it on open-and-save would destroy data the moment an existing workout was edited. `StepCard.tsx`'s collapsed summary is equally honest about it (`stepSummary`/`stepSubSummary` read `row.kind` rather than fabricating a pace reference a `wu`/`r` row never had) |
| Free-text `DUR` field (`input placeholder="1'"`) — the encoding (minutes vs. meters) is implied only by what the rower happens to type | A masked numeric-pad value field (`ClockInput`, MIN branch) paired with a two-chip **MIN / M** unit toggle (`DurationInput`), each control ≥44px | Free text left the unit ambiguous — a rower had to remember whether to type a bare number, `10'`, or `2500m`, the same category of defect (an invalid or ambiguous hand-typed value) that started this phase for PACE REF below. The toggle makes both encodings equally easy to author with nothing to remember. The M branch stays a plain integer `toSteps` bounds-checks directly; the MIN branch's field itself changed again in Phase 5F (Tasks 1–4) — digits fill right-to-left into `ss`/`mm`/`hh` and render in the house clock format (`0:45`, `20:00`, `1:05:00`) rather than a bare decimal number of minutes, so a 45-second step is typable and displays as `0:45` everywhere (the builder, the library, and the workout detail screen all now format step durations through the same `domain/duration.ts` helpers) instead of only ever being reachable as `0.75` |
| New handoff's SPM stepper: `+` from empty jumps to **18** ("Interactions & behaviour") | Ergomatic's SPM stepper (`StepEditor.tsx`, built on the shared `Stepper.tsx`) wakes at **20** instead, and clamps to a ceiling of **60** and a floor of **10 before clearing back to FREE** rather than the handoff's implied `0..40` | 20 is the wake value Ergomatic's SPM control has used since before this redesign (the now-deleted `SpmInput.tsx`'s own free-text-plus-steppers control, itself mirroring James's own rule for the wake value) — the redesign changed which control renders SPM, not what pressing `+` from empty should land on, so the value carried through unchanged. The handoff's `18`/`40` are its own prototype's picks, never reconciled against a real cadence; `60` matches `domain/validate.ts`'s actual `int(s.spm, 10, 60)` ceiling. The `10` floor does **not** carry the same "server accepts it" justification — *absent* spm is equally acceptable to the server (SPM stays optional), so `−` at 10 clears to `""`/FREE rather than sticking at 10; only the ceiling is a real server-bound clamp |
| Workout numbers everywhere (`NO. 159`, `159. Lucky Penny`, a `No.` field in the builder) | No numbers in the UI at all; `sort_order` orders the library invisibly | The number is a book-catalogue artifact. Requiring one when authoring forced an invented value, and globals and personal rows number independently so the same number could appear twice. Ordering is what the column was actually doing |
| §11's 64px accent-text `PACE REF` field, accepting free text (`2k`/`6k-2`/`2k+4`) per a named regex | A `2K`/`6K`/`MAX`/`MIN` four-chip radiogroup (`PaceRefInput.tsx`), on its own line beneath the row | Free text let a rower type `8k` and get an inline error — the defect that started this phase; a structured control makes an invalid ref unrepresentable. Four extra tappables (two base chips, two stepper buttons) don't fit inline at 390px. Phase 5G added the two effort chips (`MAX`/`MIN`) alongside the original two split chips — checking one hides the ±60 offset stepper entirely (unmounted, not just visually hidden: efforts take no offset) and the TARGET strip shows the resolved effort word (`ALL OUT`/`EASY`, `effortWord` in `domain/pace.ts`) in place of a resolved range. The chip word (`MAX`/`MIN`) and the target word (`ALL OUT`/`EASY`) are deliberately different pairs — the chip is the control's own label, the target word is what a rower would say aloud, and `MIN` spoken reads identically to "minutes" (`effortSpoken` substitutes real effort language for the accessible name) |
| New handoff's PACE offset stepper: `±1s` per tap, clamped **`−15…+30`** ("Interactions & behaviour") | Ergomatic clamps the same stepper (`PaceRefInput.tsx`, reused wholesale by `StepEditor.tsx`) to **`±60`** | `±60` is the domain's own bound (`domain/validate.ts`), already in place before this redesign — see the `PACE REF` row above, from the free-text-field defect this phase started with. The redesign changed the offset stepper's surrounding card, not its bound. The handoff's tighter `−15…+30` is the prototype's own scope, never reconciled against what the server actually accepts; matching it would make some previously-valid, already-saved workouts unrepresentable in the builder. Phase 5G's `MAX`/`MIN` effort chips sidestep this bound entirely rather than clamping harder — an effort has no offset concept at all, not a zero one |
| N/A — the handoff's TYPE group has no summary text of its own, only the four chips | The classification card's TYPE group shows a short summary word opposite its label (`TYPE_WORDS`, `builderState.ts`): AN "SPEED WORK", O2 "LOW & SLOW", AT "COMFORTABLY HARD", TR "HARD INTERVALS" | Phase 5G, James's mid-phase request, matching EXPECTED PAIN's own level word (`PAIN_WORDS`, e.g. pain 3 → "WORKING") so all three classification groups read the same way: label left, plain-language summary right. Reuses PAIN's reserved-line-box fix (`.classification-type-label-row`'s `min-height`, mirroring `.classification-pain-label-row`) so the word never shifts the chips beneath it, even though — unlike PAIN — a type is always selected and the word never actually toggles in and out |
| §11's `+ PASTE TO BULK IMPORT` toggle on the New-workout screen | Its own screen at `/library/import`, reached from a control in the Library header | James reported that mixing bulk paste into the single-entry form was confusing |
| Handoff's × on a step row deletes it immediately, no confirmation | `StepCard.tsx`'s collapsed × swaps the row's action group for an inline `DELETE?` / `YES` / `NO` confirm (`confirmingDelete` state) instead of deleting on the first tap | The × sits 44px away from the duplicate (⧉) cell in one joined action group, tapped mid-authoring on a phone — a mis-tap there must not silently destroy an already-configured step. `NO` restores the normal action group with nothing lost; only `YES` calls `onDelete` |
| N/A — the handoff has no notion of a "convenience" tap area smaller than a real control | `StepCard.tsx`'s collapsed card exposes two extra clickable areas that duplicate the 48×44 `EDIT` cell's action (`onExpand`) at less than 44×44: `.step-card-line1` (326×18, the index/summary/split line) and `.step-card-sub` (180×14, the sub-summary line) | Design doc §4a's own geometry (an **~86px** collapsed card holding an 18px summary line, a 14px sub-summary line, and a 48×44 `EDIT` cell side by side) makes a compliant-height summary line impossible without growing the card past spec. WCAG **2.5.8 Target Size (Minimum, AA)** — not 2.5.5 Enhanced (AAA), which does not apply here — requires 24×24 *or* one of several exceptions; the **Equivalent Control** exception is satisfied because the fully compliant 48×44 `EDIT` cell performs the identical action in the same card, so these two lines are a redundant path to that target, not the only one. CLAUDE.md's own rule ("every tappable ≥44×44") is flatter than WCAG and carries no such exception, so these two areas are a genuine, acknowledged violation of the *project's* rule — accepted as unavoidable given §4a's fixed card geometry, not fixed here |
| N/A — the handoff has one flat library, no notion of a rower's own workouts vs. the shared starter set | A personal (non-global) workout's library row wears a `CUSTOM` badge on its second line (`WorkoutRow.tsx`'s `.workout-row-custom`, plus a `.visually-hidden` ", custom workout" suffix on the row's accessible name) and a `SOURCE` group (`GLOBAL`/`CUSTOM`, mutually exclusive) inside the FILTER sheet (`FilterSheet.tsx`) that ANDs with every other active filter | Phase 5H: every seeded/global workout is shared (35 at the time, since regenerated into the 300-workout library), but a rower's own authored/bulk-imported workouts sat in the same flat list with no visual distinction. Filtering to `CUSTOM` with no personal workouts renders a builder-link empty state (`Library.tsx`) rather than a bare "no matches" message. Task 4 (ui-fix round) retired the old standalone `CUSTOM` chip (`FilterChips.tsx`, deleted) in favour of this `SOURCE` group — GLOBAL is now as much a filter as CUSTOM always was, not an implicit, unnamed default; `ALL`, the flat chip row's own clear-everything control, retired alongside it in favour of the token row's `CLEAR ALL` |
| `RECENT` / `NOT RECENT` library filter chips (`FilterChips.tsx`, deleted) | A `LAST DONE` group label over two cells, `<21D` / `21D+` (`FilterSheet.tsx`), same 21-day boundary (`isRecent`/`RECENCY_BOUNDARY_DAYS`, `filters.ts`) and same mutually-exclusive semantics as before | Task 4 (ui-fix round), DESIGN.md's own words: "`RECENT`/`NOT RECENT` and every adjective tried in its place (`OLDER`, `STALE`) read as a verdict on the workout" rather than naming the underlying fact (when it was last done). The boundary and the never-done-counts-as-`21D+` rule are unchanged from the retired chips — only the label and the cell shape (chips to a 2-cell grid inside the FILTER sheet, matching TYPE/TIME/PAIN's own grids) moved |
| N/A — the handoff's sheet mock says only "disables at 'No workouts match'," with no notion of what that means for the CUSTOM-filter build-one empty state above | The sheet's `Show N workouts` L1 button disables at `resultCount === 0` (`FilterSheet.tsx`) rather than staying live so a zero-result combination could be "applied" on purpose — which means the `SOURCE: CUSTOM`-with-no-personal-workouts build-one empty state (row above) is reachable only via a filter combination that was already applied before it went stale (e.g. BACK restoring a saved filter after the one matching custom workout was deleted elsewhere), never via a fresh sheet interaction landing on zero results from scratch | Task 4 (ui-fix round) followed the sheet mock's literal instruction rather than silently making the primary non-disabling to preserve the old reachability, and flagged the resulting tension rather than resolving it unilaterally. James, 2026-08-04: disabled-at-0 stands — the build-one link's narrower reachability (stale filters only, not a fresh sheet path to it) is accepted, not a bug to fix |
| N/A — the handoff has no notion of iOS's native text-selection callout (Copy/Look Up/Translate/Share) on a button's or chip's rendered text | `button`, `[role="radio"]`, `.chip`, `.workout-row`, `span.stepper-value`, `.pace-ref-display`, `.filter-token`, and the anchor-styled "buttons" (`.back-link`, `.library-import`, `.library-new`, `.button-primary`, `.button-outline`, `.tab`) get `user-select: none; -webkit-user-select: none; -webkit-touch-callout: none;` (`index.css`'s reset block, just below `.visually-hidden`) | Phase 5H, a device report: long-pressing a control in WKWebView (the iOS app's real runtime) popped the OS text-selection callout instead of activating the control — WebKit treats any rendered text node as selectable unless told otherwise. Inputs/textareas stay selectable throughout, including the `Stepper`/`ClockInput` typed value cells (`.stepper-value-input`, `.clock-input`), which share the `.stepper-value` class name with the non-editable `<span>` variant this rule targets — the selector is scoped to `span.stepper-value` specifically so it does not reach the typed variants. Left deliberately selectable: plain inline body-copy hyperlinks (e.g. `StepEditor.tsx`/`StepRow.tsx`'s "Set baselines") — those read as ordinary links, not chrome controls. `.filter-token` (Task 4, ui-fix round) is the one entry here that isn't itself the clickable element — its own remove control (`.filter-token-remove`) is a real `<button>`, already covered by the bare `button` selector; the pill's LABEL text is what still needed the explicit rule. Chromium's e2e suite can only assert the computed `user-select` value; the callout suppression itself is verified on-device by James, post-merge |
| Today's LAST THREE row: `"JUL 25 · HELD · 2/10"` (`docs/design/README.md:185`) | `Today.tsx`'s `formatLogDate` + the literal HELD/UNDER/OVER word + `{log.pain}/5` — e.g. `"JUL 25 · HELD · 2/5"` | Phase 6A: the date-plus-word format is unchanged from the handoff; only the pain figure's denominator follows this table's very first row (Ergomatic is 1–5 everywhere, not the handoff's 1–10) — rendering a literal `/10` here would be the only place in the app still speaking the un-adapted scale. An earlier draft of this row also invented `✓`/`▼`/`▲` glyphs and a "33D AGO" days-ago phrasing in place of the handoff's own date+word shape; that was a bug, not a deviation, and was corrected back to match `README.md:185` before merge |
| Confirm targets' reps-marker row: the handoff gives it the *same* ×/+ remove-restore control as every other row (dims to `#b8b2a3`, sub-line "removed") | `ConfirmTargets.tsx`'s `ConfirmStepRow` renders **no** remove/restore control at all for a `"reps"` step — every other step kind gets one | Phase 6A, a binding decision from a Task 1 review handoff: `liveSteps()`/`phases()` only repeat the block that follows a *live* reps marker, so striking the marker row would silently halve (or worse) a repeated workout with no visible cause — indistinguishable at a glance from striking any other row. Rather than trying to make that consequence "unmissable" with more UI (e.g. a dedicated confirm dialog for one row kind), the marker simply has no remove affordance; its own REPS stepper is the only way to change its effect, and every press already updates the footer's live minute recount. The removed-row visual itself also differs from the handoff's literal `#b8b2a3` dimming: a struck row gets `--surface-sunken` background plus an ink-3 struck label (6.3:1 contrast), the same token-based restyle this table's ink-4 row already applies elsewhere, extended here to a struck row's DUR/SPM/REPS field labels too (ink-4 measures only 4.48:1 on `--surface-sunken`, failing AA — caught by Phase 6A Task 5's design sweep) |
| N/A — the handoff's Confirm targets row (`README.md` §4) has no per-step SPM control at all (only the ▲▼ split nudge), and its duration stepper is stated as ½-minute steps with no distance-unit variant | Every `"w"` step on Confirm targets also gets its own SPM stepper (18–32, wakes at 20 — the same wake value and ceiling convention as the builder's SPM row, see the SPM row above), and a distance-kind step's own duration steps by 100 m instead of 30 s | Phase 6A: per-step SPM adjustment at session start was a deliberate, planned differentiation (`ROADMAP.md`'s Phase 6A goal), not an oversight — a rower's stroke rate for a given session can differ from what the workout was authored with, and confirm time is the natural place to set it before the timer starts. The 100 m distance grid has no handoff precedent to follow at all (the handoff's stepper is time-only); it steps by `validate.ts`'s own minimum distance granularity rather than inventing a new bound |
| Live timer's TOTAL LEFT ruler tick labels (`#a09a8c`) | `--ink-4` (`#6f6a5f`), not `--ink-5` (the token `#a09a8c` maps to) | Phase 6B Task 3: `#a09a8c` measures 2.48:1 against `--page` — nowhere near the 4.5:1 AA floor, even at large text (3:1). This isn't the same substitution as the `ink-4` row above (that one keeps the four-tier hierarchy for mono labels); `--ink-5` is reserved everywhere else in `index.css` specifically for `:disabled` controls, which WCAG exempts from the contrast requirement — the ruler's tick labels are live informational text, not a disabled control, so that exemption doesn't apply and the token was wrong for this use regardless of hierarchy. `--ink-4` (4.76:1 on `--page`) is the same token the timer's other small mono labels (STEP line, TARGET SPLIT/RATE headers, TOTAL LEFT label) already use |
| N/A — §6's own text models one uniform live-timer behaviour: "phase auto-advances at 0 and seeds the next phase's duration; ◀/▶ skip and re-seed", with no distance-phase concept at all | A distance phase counts UP (`elapsedSeconds`, a stopwatch) instead of down, never auto-advances (there is no fixed duration to advance against), and folds its target meters into the STEP line (`· 500M`) since §6's static mock has no dedicated slot for one. The control row itself is the SAME `56px / 1fr / 56px` three-column grid every phase kind gets (`.timer-controls`) — only the rightmost 56px slot's control changes, from `▶` to `NEXT →` (`.timer-control-next`); ◀ and Pause work identically on a distance phase. On NEXT, an elapsed reading more than 2× or under half the domain-estimated duration (`domain/expand.js`'s `phaseSeconds`) stages a Keep/Discard choice (`.timer-suspect`) instead of recording the split silently | Phase 6B: §6's text describes only time-based intervals; Ergomatic's domain has always supported distance steps, so the timer needed an equivalent with no handoff precedent to follow. An earlier draft of this decision (Task 3's first pass) replaced the whole three-column grid with a single full-width NEXT control, dropping ◀/Pause for a distance phase entirely — a spec review caught this as a real regression (a distance piece got no way to pause for a broken foot strap) and Task 3's own fix round restored the shared grid; the full-width control (`.timer-next-distance`) no longer exists in the code, only `.timer-control-next` at the same 56px width as every other rightmost control. The suspect-actual staging exists because the wall-clock design that makes lock-screen resilience possible also means a long suspend (or a mis-tap the instant a piece starts) produces a real but absurd elapsed reading if recorded uncritically — see `Timer.tsx`'s `isSuspectActual` |
| §6's own text: "past the last phase → Log session" — an unconditional transition, the same auto-advance/▶ behaviour every other phase boundary gets | `▶` (or `NEXT →` on a distance phase) on the LAST phase stages a "Finish this session?" confirm — the same `BaselineEditor.tsx`-idiom staged panel (`.timer-end-confirm`, a panel appended below, never a modal) as `END →`'s own abandon confirm — rather than completing outright | Phase 6B, a spec-review finding (Task 3's fix round): completing a run is a documented one-way door (`engine.ts`'s `isComplete` comment — there is no "un-finish"), so the control that triggers it must be a deliberate act, not a single tap under an unassuming "Next phase" label indistinguishable from advancing to phase 4 of 6. `END →`'s own confirm (itself already beyond §6's literal text, which shows only the button, not a confirmation step) established the panel idiom this reuses; a suspect-actual choice on the very last distance phase combines into the SAME single staged panel rather than stacking two confirms in a row |
| N/A — Confirm targets (§4) assumes baselines are always set ("Baselines strip '2k 1:52.0 · 6k 2:02.0'"); it has no unset-baselines state to describe | START is unconditionally blocked whenever baselines are unset: Confirm's footer swaps to the existing no-target/`/you` idiom instead of a clickable START, and Countdown (`/session/countdown`) defensively bounces back to Confirm if ever reached with unset baselines by another route (a direct or deep link) | Phase 6B, a controller decision (Task 3): the engine's `buildRun` requires a concrete `{k2Seconds, k6Seconds}` pair with no fallback — running against a dummy `{0,0}` pair would silently mis-price every target rather than fail loudly. The block is unconditional even for a workout whose own steps never reference a baseline at all (every step effort-ref, say) — one simple, predictable rule, rather than calculating per-workout whether baselines actually matter |
| N/A — Today's own handoff section has no notion of a live or completed-but-unlogged session surfacing on this screen at all | A resume card — accent-filled, above the suggestion card, the screen's most prominent element when present — renders whenever `loadRun()` finds a live run (`completedAt === null`): the workout's title, elapsed time so far, and a `Resume session` link straight back to `/session/run`. A quieter line (workout name + "unlogged session" + a real `Log it` link to `/session/log`) renders instead once the run is already complete but not yet logged — and, since Task 3 (ui-fix round), a 44×44 accent-outlined `✕` beside `Log it` that arms a staged Discard IN PLACE (the row's own contents swap to "Discard {title} without logging?" + a solid-accent "Tap again"; firing clears the draft/run records with no POST and removes the row, no navigation) | Phase 6B whole-branch review, spec Resilience #6 (F2): a cold start — the OS killing a backgrounded app, real on iOS, the native-first platform — used to land on Today with no path back into an in-progress or finished-but-unlogged session; the only route in (Start on the suggestion card) discards it, via `WorkoutDetail.tsx`'s own already-shipped staged "replace" confirm. `SessionRun` gained `workoutId`/`title` (F3a) precisely so this card never has to also load `SessionDraft` just to say whose session it is. F2's original ship kept the line honest by naming the workout without a button that did nothing yet ("6C will log it here"); Phase 6C Task 2 built that screen, so the line now links to it for real. Task 3 (ui-fix round) added the ✕: DESIGN.md's own "Items 2 + 3" calls for a Discard at every surface that can strand an unlogged session, and the resume card's own live run is deliberately excluded (the ✕ only ever renders once `completedAt !== null` — a live session still has WorkoutDetail's replace-confirm as its one discard path, unchanged) |
| §7's own mock: a fixed two-slot `PACES LOCKED AT 2K 1:52.0 · 6K 2:02.0` line, always both bases | `LogSession.tsx`'s panel renders ONLY the bases the workout's own steps actually reference — one, both, or the panel omitted entirely (no bare dash) when neither is derivable | Phase 6C Task 2, whole-branch review (F1): checked against all 35 seeded starters (the original starter library, since regenerated into the 300-workout library) and found the two-slot mock could never actually occur — 16 were 2k-only, 18 were 6k-only, and one all-effort workout referenced neither, so a fixed two-slot layout would show a permanent, unconditional dash for one half on EVERY real session logged, violating the house "never a bare dash" rule this screen's own per-step list already honors. The generated 300-workout library keeps to one base per workout exclusively — the taste pass (9b9fde5) converted AT's last remaining 2k-base refs to 6k, so zero of 300 mix both (3 did before that pass) — so the two-slot form stays a synthetic-fixture-only case: it renders for a workout that genuinely mixes both bases (a custom/bulk-imported one, or a dedicated test's synthetic fixture, since the generated library no longer has a real example), never the universal case §7's own mock implies |
| N/A — the handoff has no notion of a manual/off-app logging door at all; only the timer's own live-session flow (Countdown/Timer/SessionComplete/the Log screen's session door) ever hides the bottom tab bar | The manual door (`/library/:id/log`, `WorkoutDetail.tsx`'s "Log it after") deliberately leaves the tab bar visible, unlike the session door's own hidden bar for the identical `LogScreen` markup | Phase 6C Task 3, corrected by the whole-branch review (IMP-2): this row used to justify the difference by saying every other tab-bar-hidden screen has "an explicit in-screen way to leave without it (Cancel, END, Back to Today, and the session door's own staged Discard, respectively)" — WRONG about the session door specifically. Discard is destructive (it wipes the run/draft records), not a Cancel/END/Back-to-Today equivalent, so before the review's fix round the session door actually had NO non-destructive way to leave this screen at all while its tab bar was hidden. The real reason the two doors differ is narrower and still holds: the manual door touches no storage whatsoever (it never reads or writes the draft/run records, and nothing is staged server-side until a successful POST), so there is nothing an early exit could leave dangling and showing the tab bar costs it nothing — pinned by both a unit test (`AppRoutes.test.tsx`) and an e2e sweep (`e2e/design.spec.ts`, "the tab bar stays visible on this route, unlike the session door"). Both doors now also carry their own `BackLink` (IMP-2's actual fix, `LogSession.tsx`'s `LogScreen`/`backFallback`) on their main, ready-to-save state — a genuine non-destructive exit that exists independently of whichever way the tab bar renders |
| N/A — the handoff has no notion of the browser's own UA `<button>` chrome interacting with a specced button height at all | Every `.button-primary` button in the app (`index.css`) renders roughly 6px taller than its own specced `min-height` — neither the file's own `button` reset nor `.button-primary` itself resets the browser's default `<button>` chrome (Chromium's UA stylesheet: a ~2px outset border plus ~1px vertical padding per side), which `min-height`/`line-height` alone can't clamp below | Phase 6C Task 2 found the identical gap on `.log-save` (a `.button-primary` control with its own 54px spec, measured rendering at 60px) and fixed it there with `border: none; padding: 0` scoped to `.log-save` alone (`index.css`) — deliberately NOT applied to `.button-primary` app-wide, since that touches every screen sharing the class and is a bigger, more deliberate change than one task's own scope. Recorded here (whole-branch review, IMP-6) so the gap isn't silently rediscovered as a "new" bug on some other button later. The ui-fix round's own five-level button system (`.button-l1`-`.button-l4-armed`, Task 1) closes the gap structurally for every control that migrates onto it — `border`/`padding` are always explicit, never left to the UA stylesheet, on all five levels — which is why `WorkoutDetail.tsx`'s own "Start" (this row's original example, specced 52px, rendered 58px) no longer demonstrates the bug: it's `.button-l1` now, specced 56px, rendering 56px. Fix round 1's own F2 additionally converted WorkoutDetail's staged-delete panel (a DIFFERENT staged confirm on that same screen, "Replace session," is not converted) off `.button-primary` entirely, onto the armed-in-place L4 idiom. `.button-primary`/`.button-outline` themselves are untouched and still carry the gap everywhere else nothing has migrated yet — named survivors, not an exhaustive list: the sign-in screen's primary button (`auth.tsx:28`/`:48`), `WorkoutDetail.tsx:283`'s "Replace session" staged confirm, `Timer.tsx`'s three staged-confirm primaries (`:522`/`:611`/`:635` — END/finish/suspect), `Plan.tsx:232`/`:260`'s Reset/Switch confirms, and `BaselineEditor.tsx:151`'s Apply. (`LogSession.tsx`'s own staged Discard — the control this row used to cite at `:798` — is no longer one of these: Task 3, ui-fix round, converted it off `.button-primary` entirely onto the armed-in-place L4 idiom, the same conversion WorkoutDetail's staged-delete panel got in fix round 1; `LogSession.tsx:599`'s Save was never actually a survivor of this gap — it's the same `.log-save` control this row's own opening sentence already exempts.) Fixing `.button-primary`/`.button-outline` app-wide remains a future phase's call, not this row's |
| N/A — Today's own handoff section (`docs/design/README.md` §"1. Today") has no filter chips and no type-swap control at all; its `SWAP FOR ANOTHER [AT]` block is the `todayPick` pool-row list (SHUFFLE), a different control entirely | Two independent controls beneath the plan line / suggestion header (`Today.tsx`): AN/O2/AT/TR type-swap chips render directly on the plan line, only under an active plan, and override the day's prescribed type before `suggest()` runs (tapping the chip matching the plan's own call un-swaps) — untouched by the FILTER sheet below, which has no TYPE group of its own. Separately, a `FILTER ⌄` chip (same chip geometry as `SHUFFLE ↻`, parked beside it) opens a sheet (`TodayFilterSheet.tsx`, built on the same `SheetShell`/`CellGrid` primitives the Library's own sheet uses) holding DIFFICULTY (EASY/MEDIUM/HARD, multi-select), TIME (`<30′ 30–45′ 45–60′ 60′+`, multi-select bucket union — Amendment, 2026-08-04 PR #50 round: unified onto the Library's own four buckets, `src/components/durationChips.ts`'s `DURATION_CHIPS`, replacing the original round's single-value cap select), and PAIN (five 1–5 cells, multi-select union — same shape as the Library's own PAIN group below); below PAIN, Round 2 (2026-08-04) adds the Library's own LAST DONE (`<21D`/`21D+`, never-done counts as `21D+`) and SOURCE (`GLOBAL`/`CUSTOM`) pair, sharing one line as two half-width mutually-exclusive-toggle-off groups, both defaulting to null (no deviation until set — no pref-derived default the way DIFFICULTY/TIME get one). The primary reads the constant `Apply Filter` (Revision, mid-Round-2 — replacing an earlier `Show N options`/`Shuffle N options` draft that named a count the button no longer carries); the live pool count moved to a small mono caption directly above it (`N OPTIONS`/`1 OPTION`/`0 OPTIONS`, singular-aware), which is also the only remaining explanation of the button's disabled-at-0 state. On apply, a shown pick that still matches the new filter stays; one the new filter excludes moves to the newly filtered pool's own top choice (`suggest()`'s own `pickOverride ?? sorted[0]`, unchanged mechanic, exercised by every dimension including the two Round 2 adds). Active filters render back on the Today screen as a token row (`todayFilterTokens.ts`: one token per group that deviates from the day's defaults — LAST DONE/SOURCE deviate simply by being non-null, same range/list collapsing as the Library's `filterTokens.ts` — the TIME token now shares that exact collapse helper, `src/components/durationTokenLabel.ts`, rather than keeping a parallel copy), each token a 44px pill filling ink, plus a `CLEAR ALL` on the token row itself, present only while a token exists (CLEAR ALL resets LAST DONE/SOURCE to null alongside DIFFICULTY/TIME/PAIN's own pref-derived defaults). TIME's own token has one label the Library's never needs: an empty selection deviating from a non-empty default reads `ANY TIME` rather than a range (there is no bucket to name), distinguishing it from an EXPLICIT all-four selection (which reads the full contiguous range, `<30′–60′+`) even though the two filter identically in `suggest()` | Task 2 (Today enhancements, post-6C) shipped both controls as flat, always-on chip rows; Task 3 (2026-08-04 round) folded DIFFICULTY/TIME/PAIN into the FILTER sheet, matching the Library's own Task 4 (ui-fix round) collapse — same one-`FILTER ⌄`-chip-plus-sheet-plus-tokens reasoning: three always-visible groups took permanent vertical space on a screen whose actual job is "here's the one workout, row it," and the sheet groups them the same way the Library's does, by actual selection semantics rather than leaving them as flat siblings. The type-swap chips stay outside the sheet on purpose (James, 2026-08-04 spec): they pick which pool `suggest()` runs against, a plan-level decision, not a narrowing filter on an already-chosen pool — collapsing them in too would blur that distinction. Both controls remain ephemeral, client-only overrides (`todayOverrides.ts`, `localStorage`, keyed the same `{date, planKey, doneN}` way as `todayPick.ts`'s own pick) layered on top of the server's own preferences (`SuggestPrefs`) without ever writing back to them — visible narrowing for a single day's suggestion, not a saved preference change. The Amendment round's own James feedback on PR #50 retired TIME's original single-value cap select (`≤30′ ≤45′ ≤60′ ≤90′ NO CAP`) in favour of unifying on the Library's bucket format everywhere — `domain/suggest.ts`'s own `SuggestPrefs.durations` field mirrors `Filters.durations` exactly, and `bucketsForCap` (`domain/duration.ts` — moved there from client-only code once `server/routes/data.ts`'s own `/api/today` route turned out to need the identical derivation) maps the account's stored preference cap to the buckets it implies (60 → the first three, excluding `60+`) so the TIME group starts with a real, if approximate, default set on first load. Round 2 (James's own verbatim request, "device screenshots of both sheets") folds LAST DONE/SOURCE into the same sheet for parity with the Library's own five-group set, sharing `isRecent`/`RECENCY_BOUNDARY_DAYS` (moved to `domain/recency.ts`, the same domain-ward move `bucketFor`/`DurationBucket` made) and `LibraryEntry.isGlobal` between the two screens' `suggest()` calls; the controller's own mid-round addition (the live-count caption) keeps the honesty signal the removed button copy used to carry, flagged to James rather than assumed |
| The Library's own `CLEAR ALL` empties every filter to nothing (`clearFilters()`, `Library.tsx`) | Today's `CLEAR ALL` resets to the day's pref-derived DEFAULTS instead — every difficulty, the account's own time cap expanded to its bucket set (`bucketsForCap(preferences.timeCapMinutes)`), no pain filter (`filterDefaults`/`clearAllFilters`, `Today.tsx`) — never to an empty selection | Task 3 (2026-08-04 round), a structural constraint, not a stylistic preference: `suggest()`/`suggestFreestyle()` read `prefs.difficulties.includes(e.difficulty)` with no length escape hatch (unlike `painLevels`, which explicitly treats an empty array as "no filter" — `domain/suggest.ts`). Emptying DIFFICULTY the same way the Library empties its own TYPE selection would make every entry fail that check at once — a real zero-result pool, not a restored one. Today's CLEAR ALL resets to the explicit "everything allowed" state instead, which is what an unfiltered day's suggestion actually looks like; pinned by `e2e/today.spec.ts`'s own CLEAR ALL test asserting the card lands back on the real unfiltered pick, never an empty one. (TIME itself has no equivalent structural constraint — an empty `durations` array is already "no filter," identical to the Library's own semantics — CLEAR ALL restores the pref-derived bucket set for TIME purely for consistency with the other two groups, not because emptying it would zero the pool) |
| N/A — the Log screen's own handoff (`docs/design/README.md` §7) has no notion of a plan whatsoever, let alone a way to keep a logged session from advancing one | A single full-width toggle row (`.log-plan-toggle`, `LogSession.tsx`'s `LogScreen`) above Save, rendered on both doors only when `plan.planKey !== null`: default reads `COUNTS TOWARD PLAN · SESSION {doneN+1} OF {sequence.length}`, tapped reads `OUTSIDE THE PLAN — won't advance`. The server pairs it with an additive `advancesPlan` boolean (`POST /api/logs`, defaults to `true`) that gates only the `plan_state` upsert inside `logs.ts`'s `create` transaction — the log row itself always inserts regardless | Task 3 (Today enhancements, post-6C): a rower doing a genuine make-up session, or logging a workout twice in one day, had no way to record it without also silently advancing plan progress meant for one real session. The toggle is door-independent (`useLogForm`'s own state, not per-door) since nothing about it differs between the session and manual doors. A plan-hook error, or the plan fetch simply still being in flight, deliberately does NOT block Save or hide the rest of the form — only the toggle itself disappears until the plan resolves with an active plan — since logging a session must never be hostage to an unrelated fetch failing or stalling (fix round 2, whole-branch review M1/M2: an earlier version of this door DID gate the whole form on the plan fetch resolving, which parked a STALLED `/api/plan` request at LOADING… forever) |
| ui-fix handoff's own two-idiom button vocabulary (`.button-primary`/`.button-outline` only) | A third idiom, level 3 "commit-in-card": solid `--ink`, mono 12/600/0.16em, 48px (`.button-l3`, index.css) — the builder step editor's `DONE` and, in a future task, staged baseline `Apply` | Task 1 (ui-fix round): an in-card confirm that closes an editor without leaving the screen must not compete visually with the screen's own level-1 primary action. Ink reads as "settled/committed" without any risk of being mistaken for the screen's one accent-filled action — DONE "was already black" before this task (an un-named one-off, `.step-editor-done`), formalized here into a shared, reusable class |
| ui-fix handoff's own two-idiom button vocabulary | A fourth idiom, level 4 "destructive": surface fill, 1px **accent-outlined** border, accent label (`.button-l4`) — Delete workout, and (Tasks 2-3) Discard without logging at rest. An "armed" variant (`.button-l4-armed`) fills solid accent with cream label once tapped once | Task 1 (ui-fix round): the README's own accent usage was solid-only; a destructive control needs a danger signal (accent) without a solid block competing with the screen's own happy-path L1. Last in the stack, under a 1px `--rule` divider (`.action-stack-rule`) — visually and structurally separated from the safe actions above it |
| N/A — the handoff's own full-width-stack rule (this round) has no stated exception | SHUFFLE (Today.tsx) stays sub-full-width — 44px chip geometry (`.today-shuffle`), not a stacked block — parked at the right of the `SUGGESTED` header row (originally `SUGGESTED FOR TODAY`; shortened by the 2026-08-04 round's final fix wave, M2 — see the row below), position unchanged | Task 1 (ui-fix round), James's explicit call: SHUFFLE reads as a small utility beside a label, not a screen-level action competing for the one L1 slot. It stops being its own bespoke species regardless (was `.button-outline` wearing an ink border; now genuine chip geometry — transparent fill, `--rule-3` border, mono 11px/0.14em `--ink-1` label, dashed border when disabled) |
| N/A — same rule, same round | Transport (`◀ Pause ▶`) and steppers (`− +`) stay in a row, exempt from the full-width-stack rule | Task 1 (ui-fix round): both are one compound control (three positions of a single "advance the session"/"adjust one number" action), not several independent actions competing for stack position — stacking them full-width would misrepresent them as separate choices |
| Today's own accent-filled active type chip (a pre-existing bug the DESIGN.md handoff calls out by name) | Today's type-swap chips fill with THEIR OWN type colour when active (`--type-an/o2/at`, `--type-tr` = `--ink`), identical to Builder's ClassificationCard — never a flat accent regardless of which type is active | Task 1 (ui-fix round): DESIGN.md's selected-state fix — "accent now means exactly four things... it no longer means selected." Today and Builder now render the identical chip for the identical type, whether the rower is filtering or authoring. Builder's own TYPE chips were already correct before this task; Today's were the bug |
| DESIGN.md/this round's own task-1 brief cite `--ink-1` for SHUFFLE's label; `tokens.css`'s ink scale has always started at unsuffixed `--ink`, with no `--ink-1` | `--ink-1` added to `tokens.css` as a literal alias of `--ink` (`#1b1a17`) | Task 1: rather than silently substituting `--ink` at the one call site the handoff names, the token now exists under the name both handoff documents actually use — closes the gap for future readers of either document |
| Builder-redesign handoff's own EXPECTED PAIN section: the selected cell fills its own per-level ramp colour (`--pain-ramp-1..5`) | ClassificationCard.tsx's selected PAIN chip fills plain `--ink`, the same rule DIFFICULTY already used — no inline style, no ramp colour | Task 1 (ui-fix round), DESIGN.md's own finding: "Builder's gold pain selection goes" — `--pain-ramp-3` (`#8a5f18`) is the IDENTICAL hex to `--type-at`, so a mid-level pain selection could read as an AT-type mark. `--pain-ramp-1..5` stay defined in `tokens.css` — `LogSession.tsx` (a different, untouched-this-round screen) is still a live consumer |
| Every split-ref target shown as a `lo–hi` tolerance band (Detail's `StepRow`, Builder's TARGET strip, Confirm's target rows, Timer's UP NEXT and its own TARGET SPLIT sub-line) | Every one of those surfaces shows the single EXACT resolved split instead (`fmtSplit`, no range). The Timer's sub-line — the one surface that lost information, not just format — now shows the REF the split was resolved from instead (`refLabel(ref).toUpperCase()`, e.g. `6K +16`), keeping the card's three-line rhythm | Task 2 (ui-fix round), DESIGN.md's own Item 1 verbatim: "Straight swap to the single resolved split everywhere the band appeared" and "`toleranceRange()` stays in the domain and keeps feeding off-target nudge judgments — only its display call sites change." (Fix round 1: that second clause doesn't hold up against the code — a repo-wide search found no reader of `toleranceRange()`'s `.lo`/`.hi` anywhere, past or present; the sentence is DESIGN.md's own stated rationale, not a claim this row independently verified.) Fix round 2 (this wave), James's ruling (Option A): `toleranceRange()` (`domain/pace.ts`) is now DELETED, along with `phases()`'s inert `_tol` parameter, `buildRun()`'s `tol` parameter, `Countdown.tsx`'s `readPaceTolerance`, and `tokens.css`'s unwritten `--pace-tolerance` token — the repo-wide search this row already describes found no reader of `.lo`/`.hi` anywhere, so "stays in the domain and keeps feeding off-target nudge judgments" never described anything that existed. The server's own `paceToleranceSeconds` preference is untouched; see the row below. Every `.label` call site was one of the four display surfaces above (`domain/expand.ts`'s own phase-label builder turned out to be a fifth, reaching display via `Timer.tsx`'s `upNextText`/`thenNextText` and `logDraft.ts`'s `buildLogSteps` fallback path — traced and fixed the same way). `Phase`/`EnginePhase` gained an optional `ref` field (the raw `PaceRef`, already nudge-effective) so the Timer's sub-line can call `refLabel` without reaching back into the `SessionDraft` the way `logDraft.ts`'s own LABEL IDIOM comment used to have to. Effort/rest/warm-up phases are unaffected — their target was always a word, never a number |
| The handoff's own State section names `paceTolerance` (0–3s, default 1) and `accentColor` (default `#b5341f`) as "configurable design props," treated as settings rather than hard-coded values | Both persist server-side as real preference fields (`preferences.pace_tolerance_seconds`, `preferences.accent_color`; `PUT /api/prefs` validates and echoes both, 0–10 for tolerance) but are read by nothing client-side — neither is even in `usePreferences.ts`'s own `PreferencesData` type, and a repo-wide grep for `paceTolerance`/`accentColor` outside the server layer returns nothing | Fix round 2 (whole-branch review, James's ruling, Option A): a coherent family of "settings the client hasn't built yet," not two unrelated orphans — Phase 9's own backlog already lists "Pace tolerance (0–3 s) and accent color as real settings." Removing the columns would need a destructive migration for fields a shipped iOS build may already be sending, for no gain; keeping them documented here means the next reviewer doesn't re-litigate whether they're dead code. The client-side chain that used to READ tolerance (`toleranceRange`, `phases()`'s `_tol`, `buildRun()`'s `tol`, `Countdown.tsx`'s `readPaceTolerance`, `tokens.css`'s `--pace-tolerance`) is deleted — see row 52 above — since it had zero importers outside its own unit test and fed a token nothing ever wrote |
| Mockup's timer sub-line shows a zero-offset ref with an explicit `+0` (`6K +0`) | `domain/pace.ts`'s `refLabel` renders a zero offset as the bare base instead (`6K`, no `+0`) — shared verbatim with the log-step chips' identical rule (`logDraft.ts`'s `refPaceLabel`) | Whole-branch review L3, James's ruling 2026-08-04: leave as-is. `refLabel` is one function shared by the Timer's sub-line and the already-shipped log-step labels, so adding `+0` would also change existing log-step copy and its pinned tests for a cosmetic zero — not worth the churn |
| N/A — the handoff's own selected-state fix (row 49 above) only ever describes Today's type-swap chips; it never states what `--type-tr` itself resolves to as a token | `--type-tr` aliases `var(--ink)` (`tokens.css`), not its own colour and not `--accent` | Fix round 1, James's ruling: `--type-tr` used to be the literal hex `#b5341f` — IDENTICAL to `--accent` — so every TR badge/chip (Library rows, LAST THREE, Plan, Today's type-swap chip, Builder's TYPE chip) filled with what was structurally "accent," quietly breaking this round's own "accent means exactly four things, never a fifth type" rule for one type out of four. TR now repaints app-wide to read as ink everywhere, the same colour `--type-test` already used (`TypeBadge.tsx` pairs it with `--on-color` text, the identical 17.1:1 pairing TEST badges already prove safe). This is a deliberate, app-wide visual change, not collateral — recorded here because two authorities (this file's own row 49 and `docs/design/README.md`) still asserted `--type-tr = --accent` after the fix shipped |
| `docs/design/README.md`'s own Screens section (§1, frozen handoff record): the suggestion header's state label reads `SUGGESTED FOR TODAY` (or `YOUR PICK`) | Today's suggestion header label (`Today.tsx`) reads plain `SUGGESTED` in both plan and freestyle modes — never `SUGGESTED FOR TODAY` | 2026-08-04 round, final fix wave (M2), the controller's adjudicated ruling: at 390px, `SUGGESTED FOR TODAY` plus the `FILTER ⌄`/`SHUFFLE ↻` chip pair (Task 2, same round) no longer fit `.today-suggestion-actions`'s available width and wrapped to two lines — a regression this round introduced (the label sat alone, full-width, before the chips joined it). "FOR TODAY" was redundant on a screen whose only subject is today, and freestyle's own suggestion state already used the shorter `SUGGESTED` — so the fix drops the plan-mode-only branch entirely rather than reflowing the header, and both modes now share the one label |

Superseded by the builder redesign (`docs/design/builder-redesign/`, Phase
5E): the permanently-expanded row list (`StepRowEditor.tsx`, deleted this
phase) is replaced by an accordion — one card open at a time
(`StepCard.tsx`/`StepEditor.tsx`). The Save button reads **`Save to
library`**, the handoff's own label: an earlier `Save` shortening was tried
and reverted this phase at James's request, so there is currently no
deviation to record for it. Four rows previously listed above are retired
the same way — either reconciled into a row further up (against this
redesign's own handoff, not the pre-redesign one) or summarized in a bullet
below, so the table only ever describes what's actually shipped:

- The SET cell's old clone-button replacement (`↻`, `aria-label="Duplicate
  Row N"`) is gone along with the permanently-expanded row-list screen it
  belonged to. `StepCard.tsx`'s own collapsed `⧉` (`aria-label="Duplicate
  Step N"`) and `StepEditor.tsx`'s expanded `DUPLICATE` are its
  replacements; the reasoning for deriving the repeat span rather than
  marking individual rows (a non-contiguous mark set can't round-trip
  through the domain's single `reps` marker) still holds and needs no
  separate row of its own — nothing about the redesign changed that model.
- The `§11` column-header strip and its per-field-affix replacement (a
  visible `DUR`/`REST`/`SPM` label beside each control instead of a strip
  above the row) no longer apply: the accordion's collapsed card has no
  header at all (just a two-line summary), and the expanded editor's own
  per-row labels (`.step-editor-row-label`) are this redesign's own spec
  (§4b), not a workaround for a header that stopped fitting.
- The free-text-plus-steppers `SpmInput.tsx` (and its own 40px-tap-target
  fix, since the free-text field it wrapped was narrower than the 44px
  steppers beside it) is deleted; SPM is now a `Stepper` built directly into
  `StepEditor.tsx`, uniformly 44px like every other control. Phase 5E first
  shipped this as a bare stepper (no typable field at all, per the
  redesign's own SPM row spec) — Phase 5F (Task 5) reverted that part: the
  value cell became a typable numeric input again (`Stepper`'s optional
  `onValueChange`/`valueInput` props, both undefined for REPEAT and PACE,
  which stay bare spans), because a bare stepper could only reach a given
  value or clear back to FREE one tap at a time — reaching `27` from empty,
  or clearing an already-set value at all, took several presses (walking
  all the way down to the 10 floor to clear it). REST got the same typable
  treatment in the same task, via `ClockInput`. A typable empty cell can no
  longer render "FREE"/"NONE" as literal text the way the old bare-span
  reading did, so both value cells lost that reading for one task's length —
  Task 9 restored it as a `placeholder`, styled through `::placeholder` at
  `--ink-4` (5.29:1 against the shared `--surface` background) rather than
  the field's accessible name, which stays the `aria-label` set alongside it.
  Its wake-value/bound departure from the redesign's own handoff is recorded
  in its own row above now, rather than repeated here.
- The five-cell SVG-face pain picker (`--pain-1`…`--pain-5` and their
  `-fill` counterparts) is gone — `PainPicker.tsx` and those tokens were
  deleted this phase, replaced by `ClassificationCard.tsx`'s numerals-only
  EXPECTED PAIN control on the separate `--pain-ramp-1..5` family. This
  isn't just superseded, it's reconciled: the redesign handoff itself
  (`builder-redesign/README.md`, EXPECTED PAIN section) explicitly drops
  the ink-stroke faces, so numerals-only is no longer a deviation from the
  (current) handoff at all.

Everything else in the handoff (palette, type, spacing, 44px targets, AA,
2px radii, screen structures, pace math, timer behavior) remains binding —
except where a numbered row above records a shipped divergence. Pace math's
own *resolution* rules (what a ref resolves to, how nudges apply) remain
binding; its *display* rule (a band vs. a single exact value) is superseded
by row 52. This table is the current-state authority throughout;
`docs/design/README.md`'s own "Screens" section is a frozen handoff record,
not re-verified against this table line by line — see that section's own
header note.

## Domain spec deviations (non-UI)

This table is separate from the UI/UX handoff table above — it records
divergences from `docs/superpowers/specs/2026-08-05-phase-7a-monitor-domain-design.md`'s
own §2 type block instead, using the same three-column format.

| Spec shows | Ergomatic builds | Why |
|---|---|---|
| §2's `IntervalActual.index: number` (verbatim) | `IntervalActual.index: number \| null` (`domain/monitor/types.ts`) — `null` when `domain/monitor/pm5/intervalIndex.ts`'s `toActualIndex` cannot explain the machine's reported index against the armed program's length | Phase 7A-fix Task 3 review (D3), narrowed by Phase 7A-fix-2 Task 5: `IntervalActual.index` is normalized by `toActualIndex` (0x0037/38's own function since Task 5 — `toProgramIndex` stays `MonitorFrame.intervalIndex`'s, for 0x0033, unchanged; the two now mirror each other's boundary shape — one step outside either end clamps, more than one is `null` — but apply the offset under different conditions: `toProgramIndex` only while resting, `toActualIndex` always). `toActualIndex`'s own guards (`intervalIndex.ts`, byte-identical shape to `toProgramIndex`'s three return sites at `:167`/`:168`/`:180`) return `null` for: no program armed (`programLength <= 0`) — unreachable from `src/monitor/driver.ts`'s `emitIntervalComplete` in practice, since that function only ever calls `toActualIndex` past its own separate out-of-run gate, which handles "no program" itself by skipping `toActualIndex` entirely and logging `"boundary-out-of-run"`, not `"divergence"`; a state outside `rowing`/`resting` (most reachably `"terminated"`, CSAFE-DEF footnote 12); and a candidate more than one step outside the program's valid range — the genuine divergence (re-review MUST-1 rejected an earlier version of `toActualIndex` that clamped this case unconditionally instead, silently fabricating a plausible-but-wrong interval identity). Forward attribution itself is NOT a `null` case: the offset rule ABSORBS one step of it by clamping — the observed phantom index `2` normalizes to `1`, it never surfaces as `null`. The two genuinely reachable `null` causes ARE logged, forked into two distinct `"divergence"` detail strings by `emitIntervalComplete`'s own `stateActive` check (`driver.ts`) — `maybeEmitFrame`'s mirror for 0x0033 logs the same way, unconditionally on an active state, since Task 4. The type was originally kept non-nullable and the unexplainable case fell back to a fabricated `0` — reviewed and reversed, because Phase 7C prefills a rower's workout log from these actuals, and a fabricated `0` produces a log entry that *looks like* a real "interval 0" and is wrong, which is worse than a `null` a consumer must explicitly handle. The two alternatives considered and rejected: dropping the actual from the array entirely throws away real duration/split/spm averages over what is only an index problem; inventing an `"uncertain"` sentinel value invents a whole new type for one field. Doing this before 7C is built is far cheaper than reconciling it after a log screen already assumes `index` is always a number. |
| Both `2026-08-05-phase-7a-fix` Task 2's brief and `2026-08-06-phase-7a-fix-2`'s plan (Task 3's own file list) say `ProgramRejectionReason` and `DriverOptions` belong in `app/domain/monitor/types.ts` | Both stay in `app/src/monitor/driver.ts`, where fix-1 Task 2 (`360c8f6`, "feat: program() proves it, then says so") originally put them — `types.ts` never gained either type | The plan line has now been copy-forwarded twice (fix-1 Task 2's brief, fix-2 Task 3's brief) and disregarded twice, with no review ever objecting to the `driver.ts` location it actually landed in. On the merits, independent of precedent: `domain/monitor/types.ts` frames itself as design spec §2's verbatim type block plus one recorded deviation (the row above) — neither `ProgramRejectionReason` nor `DriverOptions` is part of §2; `DriverOptions` is literally the third parameter of `driver.ts`'s own `createPm5Driver` constructor, so moving it into `domain/` would invert the src→domain dependency direction this phase's layering rule exists to protect; and nothing outside `driver.ts` and its own test imports either type. Ruled CORRECT at Phase 7A-fix-2 Task 3's review — this row exists so a third brief stops repeating the error. |
