# NF-RECOVERY-SETUP-v3 — no-install idle check

Status: COMPLETED — PASS. See [the result](ZERO-SCAN-SETUP-V3-RESULT.md).
No install or scan occurred; cleanup verified in 1m42.366s total. This run
and ready permission are consumed. The procedure below is retained history.
This is one bounded experiment to establish whether the already-installed
recovery diagnostic's authenticated You page, console capture and idle reload
work together on the physical phone. Setup v2 installed and verified the app,
then stopped before this evidence existed. Its last DOM state was not retained.

## Exact operator block

> Reply **ready**, then unlock your iPhone and leave it unlocked for this
> five-minute check. No erg, scan, app taps or further reply is needed.

One readiness reply and one ordinary unlock. Zero installs, NFC starts,
PM5 actions, rowing, heart rate, pasted commands, photos or receipt copying.
The five-minute total starts at that reply and includes unlocking, host setup,
launch, waiting, capture and verified cleanup. No timer waits for another reply.
Prior desk preparation is additional time, not part of this future cap.

## One case and its observables

Use the unchanged signed recovery app and existing capture controller from
[the command card](RECOVERY-COMMAND-CARD.md). Copy the v2 install receipt only
as provenance into a fresh private directory. Obtain a fresh app listing and
require the pinned 0.23.0/789 and exact installation URL before capture launches.
A mismatch stops; it never authorizes a reinstall. Record a new original
start/deadline; no prior directory, process listing or PID is reusable.

Require the live capture host and WebView-loaded line, then a fresh process
listing binds the exact installed executable to one PID. Run idle Inspector
once with the reviewed helper hash in [the loading fix](INSPECTOR-LOADING-FIX.md).
It navigates via the actual You link if needed, waits for the asynchronously
loaded probe within its existing 45-second action allowance, checks all idle
controls, requires four complete unique canary lines in the host capture,
reloads once and requires a later document time origin in the same native
process. It never presses an NFC sample control.

Ordinary unlock is an established phone action; it starts with the lock screen
and no NFC sheet. No new gesture is requested. Actual authenticated route
readiness, physical console completeness and idle document reload remain the
explicit feasibility questions. Simulator proved Inspector selection and the
component controls with synthetic native callbacks; it did not prove this
phone route. The host loading regression passed focused tests, not a new
physical demonstration. Failure or absence of any required evidence stops.

PASS requires current install identity, actual enabled controls, complete
canary, a new document in the same process, zero NFC starts and verified
cleanup. A stopped/missing check is inconclusive; any unexpected NFC start is
failure. Reserve the final 45 seconds for cleanup, signal only the verified
live capture host PID once, and require terminal cleanup evidence. One case,
no retries, repair, reinstall, login request or extra phone instruction.
If the phone is locked or authentication/navigation is unavailable, stop and
release James. Never ask him to wait while debugging.

A PASS only clears phone preparation. The four-case recovery protocol still
requires its PM readiness verdict and separate explicit scan consent.

## PM readiness disposition

`/root/walk_pm`: the exact no-install, one-reply/one-unlock, five-minute
operator protocol passes. Its sole dispatch condition was closure of the
narrow loading guard with final code-review PASS and pinned hashes; both are
recorded in `INSPECTOR-LOADING-FIX.md`. PM explicitly required no further round
when those changes preserve this scope. The condition is satisfied. No new
design, build, installation or unchanged suite is required before requesting
the ready reply. Actual phone setup remains the feasibility experiment.
