# NF-RECOVERY-SETUP-v1

Status: COMPLETED — DO NOT EXECUTE AGAIN. One authorized install succeeded;
idle Inspector readiness remained inconclusive. See `ZERO-SCAN-SETUP-V1-RESULT.md`.
The one-install budget is consumed; no further install or scan is authorized.

Purpose: determine whether Codex can select, operate and reselect the existing
probe document through Web Inspector without operator console typing. This is
an explicitly bounded, zero-scan control-feasibility preparation. It proves
idle document control only; live NFC-sheet control remains a separate question.

## Artifact and known preparation

Use only the signed artifact pinned in `recovery-artifact-identity.json`.
`RECOVERY-PREPARATION.md` records the build, actual wireless service discovery,
scoped tests and current mismatched phone installation. The WebView bundle is
byte-identical to v8 and its metadata is prefilled. Native source carries the
approved DEBUG hold/release overlay; product implementation is absent.

The current controller includes `c31424fa` and rejects stale identity without
app mutation. Reuse its installed-app verification, attached console and
bundle-scoped cleanup. Its ordinary-normal classifier is not the readiness
oracle for a zero-scan run; retain its result without promoting it to a normal
sample. No broad unchanged suite is repeated.

## Operator cost and consent

Zero rowing, heart rate, PM5 actions, tag presentations, pasted commands,
receipt copying and photos. One ordinary phone unlock and one explicit
installation-permission reply, in this exact order and grouped block:

> Reply **install** to authorize this one diagnostic installation, then unlock your
> iPhone and leave it unlocked for the five-minute zero-scan check. No further
> reply is needed.

No erg presence or NFC sheet is requested. Before the permission reply, no
physical setup or timer is requested. The unlock happens after that reply;
it is included in the total cap, rather than hidden before it.

One install maximum; ordinary unlock is the declared starting-state correction.
The lockState fields do not prove current unlock. Total cap: five minutes from receipt of explicit install
permission through phone unlock, waiting, install, independent listing,
capture, control probe and verified cleanup. No timer waits for a human reply. If the phone is locked or
unavailable, stop and retain the result; do not start an unlock/chat loop inside
the clock. There is no retry, second install, NFC action or repair during this
bounded run. All desk preparation time is reported separately as actual cost;
the five minutes is not presented as the cost of the whole NFC effort.

## Controller-owned sequence

1. After explicit permission, record start/deadline. Reverify every prepared
   artifact file against the retained manifest and strict/deep signature.
   Install that exact path once using the previously verified devicectl
   install command, then independently list apps. Retain installation URL,
   bundle/version/build and compare them before launching anything.
2. In a fresh private `zero-scan-preflight` directory retain the install and
   listing outputs. Run the existing capture controller with the ORIGINAL
   five-minute deadline. Require live host process and actual WebView-loaded
   console line. A locked phone or failed launch ends this attempt.
3. Save an unfiltered process listing. The Inspector helper binds the exact
   executable from the verified installation to one process. Select only that
   process's Ergomatic WebView at `capacitor://localhost`; refuse ambiguous or
   already-inspected pages. Library logging stays disabled, debugger stays off.
4. Read the DOM. If needed click the existing YOU link, then positively require
   `/you`, the mounted probe, one enabled normal control and all four enabled
   recovery controls. Do not click any sample control. This action is the
   feasibility question, not an assumed prerequisite.
5. Reload the idle document once; detach/reselect, keep the same verified app
   process, require a later performance time origin and all controls ready.
   A missing document or rejected evaluation is INCONCLUSIVE. Never invoke a
   hidden control, fabricate a receipt, start a reader or ask James to repair it.
6. Signal only the retained live controller PID to finish. Require its verified
   bundle cleanup and inspect the complete console for zero reader begins,
   zero NFC starts and zero receipt attempts. Retain all evidence privately;
   commit only allowlisted observations, artifact identity and outcome.

No manual app tap through a native sheet appears in this run. Idle document
reload here is not the recovery probe's transfer operation and grants no
recovery criterion. The maximum private Inspector operation is 45 seconds;
every library call is bounded within it. Forty-five seconds are reserved by
the existing capture controller for cleanup, within the original total cap.

## PM disposition

The first draft was NOT READY because it requested no unlock despite a known
Locked launch failure. This revision replaces that contradictory prerequisite
with one declared unlock AFTER the permission reply. The original total clock
therefore includes unlocking, with no timer across a required human reply.
Native/host code review and build verification are complete. `/root/walk_pm` returned PASS
on this exact amendment. The known lock prerequisite is addressed without
another acknowledgement. Actual DOM/reload control remains the experiment; no
NFC or recovery walk is approved. There is still no install permission.

## Outcome

PASS: exact installation verified, controls read and benign navigation/reload
observed on the same app process, capture live, zero NFC begins and cleanup
verified. FAIL: an observed wrong target/action, unexpected NFC start or cleanup
failure. INCONCLUSIVE: locked/unavailable phone, missing/occupied target, missing
controls, capture failure or elapsed cap. Every outcome stops this run.

A PASS permits preparing the grouped recovery runsheet; it does not authorize
its hardware test. Another installation always needs new explicit permission.
