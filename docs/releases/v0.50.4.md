# v0.50.4 — NFC foreground recovery

James authorized merging green PR #467 and releasing on 2026-09-17.
PR #467 merged as `1f886f71` after exact-head CI passed: 9,221 tests,
603 browser tests with zero retry recoveries, and all required checks.
The recovery helper has 100% statements/branches/functions/lines coverage.

## Release range

`git log v0.50.3..origin/main --oneline` at `1f886f71` yields five merges:
#467 is covered by both release items; #463, #464, #465 and #466 change local
validation tooling and need no tester-facing item. No NFC patch or dependency
change occurs in this range, so the conditional Swift patch gate does not apply.

## Notes validation

Fast path: one product content file, two existing browser assertion files;
no domain, server, stored shape, auth or device behavior changes. No fresh
PM/DBA/mechanism review or screenshots are required for these release words.
The substantive behavior reviews and mutation gates belong to #467.

Both roots installed frozen dependencies. The real hook rejected Node 1,
then normal pre-commit passed staged format/typed lint, full typecheck and
E2E membership 30/30 (receipt `db8fdac9-9573-4f23-bf2e-b06bfd28b07a`).
The updated browser expectations first failed against the old notes: expected
v0.50.4 but received v0.50.3; expected 57 entries but received 56.

At `a98e2c12`, a unique deciding-source mutation replaced the new note's
version with `v0.0.0-nfc-release-mutant`. The real Compose build succeeded
and the News-at-rest browser assertion rejected that rendered version.
Sources were restored byte-for-byte. `pnpm e2e news.spec.ts` then passed
all 12 tests; the oldest v0.4.0 assertion remains at index 56. Stack
`ergomatic-70835`, its network and its pgdata volume were removed.

The normal push hook and full hosted CI certify the notes PR head. The tag
must follow its merge and main's successful deployment. The release command
is `pnpm ios:release` from the exact annotated tag; upload and processing
results are checked separately. No phone installation is authorized or performed.
