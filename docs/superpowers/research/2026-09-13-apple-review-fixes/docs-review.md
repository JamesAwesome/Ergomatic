# Documentation review against `86ed0636`

Status: **DONE_WITH_CONCERNS**. Read-only review of current documentation, `.env.example`, and standing-record changes. No app source, tests, runtime, device, browser, provider, or archived expiry work was reviewed or run.

## Material corrections

1. **HIGH — the native hardening record still prescribes the rejected truncation.** `docs/superpowers/research/2026-09-13-apple-review-fixes/native-logging-harden.md:36` calls 4,068-character truncation “Ready-to-paste”; `:56-58` makes a bounded line part of the proof; and `:68` repeats it in the proposed ledger entry. These contradict the controlling disposition at `:77-79`, the accepted standing ledger at `.claude/agents/antagonist-ledger.md:10742-10747`, and the handoff at `docs/superpowers/research/2026-09-13-apple-review-fixes/report.md:23-25`, all of which require full-message behavior. Remove truncation/bounds from the operative finding, oracle, and proposed entry. Also replace ambiguous “bounded level/message” / “bounded native message” in `native-logging-fix-research.md:8,29` with bounded level plus full message.

2. **HIGH — the amended cancellation spec is internally inconsistent.** `docs/superpowers/specs/2026-09-12-apple-signin-design.md:221-225` and its lifetime table at `:407-414` correctly retain cleanup authority after an unacknowledged cancel. The still-operative sentence at `:295-306` says the native operation discards in-memory credentials “on every terminal outcome,” which would erase the retry authority when the UI enters the required failure state. Narrow it to acknowledged/confirmed terminal outcomes and explicit local teardown; retain cleanup authority after uncertain cancellation.

3. **MEDIUM — the new native navigation pointer does not preserve all accepted requirements after forbidding reuse of the old codebook.** `docs/superpowers/plans/2026-09-13-apple-native.md:5-12` gives only the broad bridge/logging shape, while `:16` says the historical blocks must not be reused. It neither links the accepted research/hardening documents nor carries their operative details: dev-only app sink, immutable instance-owned sink, full messages, six-level fallback, return-none without resolve/reject, credential-free Apple rejection payloads, and the compiled WKWebView-to-stdout oracle. It also drops the exact `.fullName`/`.email` request and `com.apple.developer.applesignin = [Default]` requirements preserved only in the historical plan. Add local links and a compact current contract so future work does not have to infer requirements from current implementation or forbidden historical blocks. The deployment replacement at `docs/superpowers/plans/2026-09-13-apple-deployment.md:5-19` does preserve its operative requirements through `docs/deploy.md`, `.env.example`, and `docs/RELEASING.md`; no equivalent loss was found there.

4. **MEDIUM — the umbrella plan still issues an impossible/stale execution instruction.** `docs/superpowers/plans/2026-09-13-apple-signin.md:29` says the blocked code lens will not be repeated, but `:41` still says “Complete the interrupted code-lens.” Replace that checkbox with a historical incomplete/handback-gate statement. This preserves the incomplete verdict without directing another agent to resume excluded work.

## Checks with no correction

No operative `FRONT_DOOR_ENABLED` behavior remains: `docs/deploy.md:133-140` correctly tells operators to remove it, and the immutable deployment pointer labels it superseded. The current cancellation contract in `docs/superpowers/plans/2026-09-13-apple-auth-contract.md:31-39`, `.env.example`, Apple developer-team/relay guidance, DBA measurements, and standing-record provenance are internally consistent. A static scan found every changed local Markdown target and anchor present. Apple’s linked primary documentation supports the team-scoped identifier, team-scoped relay address, account-settings retrieval, and transfer-migration statements. Final implementation evidence is explicitly in progress and was not demanded here.

## Controller disposition

All four documentation corrections are folded. The public native report now states the accepted full-message contract at each operative point and retains the rejected truncation only as disposition history. The lifetime prose distinguishes discarded provider tokens/codes from retained cleanup authority; a UI failure state is not itself proof of a remote terminal outcome. The current native pointer carries the exact entitlement, scopes, logging/plugin contract and compiled oracle with local research/evidence links. The umbrella plan records the original blocked review as incomplete without instructing another agent to resume it. This was one docs-only pass; no repeated runtime or hardening review was requested.
