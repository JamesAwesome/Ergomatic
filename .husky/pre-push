. "$(dirname "$0")/common.sh"
# One owner discovers and executes the exact protection union. Git's real
# ref stream stays on stdin; inspection never substitutes for this gate.
exec node app/scripts/local-work.mjs run pre-push
