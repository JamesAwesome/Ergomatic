# Apple plan hardening — mechanism findings and correction receipts

The original lens 1 verdict remains BLOCKED in `mechanism-report.md`; it was not rerun or relabelled PASS. This is the controller’s disposition of the single owner fix round. The separate prescribed-code lens follows these corrections; there is no third hardening pass.

| Finding | Correction | Source and deciding evidence |
| --- | --- | --- |
| F1: stale callback cleanup erases a winner | Failure discard compares the observed immutable binding and stage/version snapshot; cleanup may clear the cookie only when it owns that snapshot. Explicit holder cancellation remains separate. | Server `259882ba`, `../../plans/apple-server-evidence/report.md`: eight held HTTP/Postgres loser cases preserve winner/grant/cookie and completion; original reviewer probe now preserves confirm/version 3; seven mutants fail then restore. Independent held SQL proof and original deadlock replay pass in `../2026-09-13-apple-db-discard/report.md`. |
| F2: repeated native target action and stale cancel cleanup | One operation owns the import/initialization/provider/proof/finalize lifetime; rendered target action reports busy; ownership is rechecked after awaiting cancellation. | Client `0f990a68`, `../../plans/apple-client-evidence/`: rendered native target/held HTTP and held cancel cases; singleflight, rendered busy, initialization and stale cleanup mutants fail then restore. Original reviewer probes pass. |
| F3: vendor bridge logs proof in Debug | The app configuration sets Capacitor logging to none; a privacy test drives the installed JS bridge from synced/built config and verifies the installed native config mapping. | Native `81ce6040`, `../../plans/apple-native-evidence/f3-native-logging-report.md`: synthetic credential appears with Debug mutant, disappears after restoration; ordinary application diagnostics still forward; unsigned iOS 15 Debug build passes. No native runtime observation claimed. |
| F4: uncertain commit acknowledgement promises no change | Generic failure and expired/consumed attempts say “We couldn’t confirm the result. Check your sign-in methods and try again.” The methods screen requests current authoritative status. | Server `259882ba` real finalize commits before socket drop and retains link/grant; client `0f990a68` consumer refetches methods and shows uncertainty with both methods Connected. Browser copy mutation fails; restored named test passes. Parent and author inspected portrait/landscape captures. |

R1 remains a nonblocking conditional callback-ID collision concern. The native plan now limits its UUID claim to native-attempt ownership. The injected equal-ID probe does not establish attacker control, a natural collision or a device exploit. The whole-branch review and final James handback receive this limitation; no new lifecycle mechanism or roadmap row is silently invented.

All evidence is candidate-specific. These scoped checks do not replace final integrated coverage, native compilation, named browser gates, exact-head CI, independent task/whole-branch review, DBA PR or PM final-PR gates. Real Apple configuration, phone authorization and native/web subject continuity remain untested runtime gates.
