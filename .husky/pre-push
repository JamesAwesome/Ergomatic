. "$(dirname "$0")/common.sh"
# One owner runs the existing three populations serially. The selection
# increment will replace their overlap/fallback; there is no dry-run bypass.
exec node app/scripts/local-work.mjs run pre-push
